- en: 2. Hands-on with a Single Neuron
  id: totrans-0
  prefs:
  - PREF_H1
  type: TYPE_NORMAL
- en: After reading about learning with neural networks in the previous chapter, you
    are now ready to explore their most fundamental component, the neuron. In this
    chapter, you learn the main components of the neuron. You also learn how to solve
    two classical statistical problems (i.e., linear regression and logistic regression)
    by using a neural network with just one neuron. To make things a bit more fun,
    you do that using real datasets. We discuss the two models and explain how to
    implement the two algorithms in Keras.
  id: totrans-1
  prefs: []
  type: TYPE_NORMAL
- en: First, we briefly explain what a neuron is, including its typical structure
    and its main characteristics (e.g., the activation function, weights, etc.). Then
    we look at how you can formally express it in a matrix (this step is fundamental
    to obtaining optimized codes and exploiting all TensorFlow and NumPy functionalities).
    Finally, we look at some code examples in Keras. You can find the complete Jupyter
    Notebooks discussed in this chapter at [`https://adl.toelt.ai`](https://adl.toelt.ai).
  id: totrans-2
  prefs: []
  type: TYPE_NORMAL
- en: A Short Overview of a Neuron’s Structure
  id: totrans-3
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: Deep learning is based on large and complex networks made up of a large number
    of simple computational units. Companies at the forefront of research are dealing
    with networks with 160 billion parameters [1]. To put things in perspective, this
    number is half of the number of stars in our galaxy or 1.5 times the number of
    people that ever lived. On a basic level, neural networks are large sets of differently
    interconnected units,^([1](#Fn1)) each performing a specific (and usually relatively
    easy) computation. They remind me of Lego building blocks, where you can build
    very complicated things using elementary and fundamental units.
  id: totrans-4
  prefs: []
  type: TYPE_NORMAL
- en: 'Due to the biological parallel with the brain [2], these basic units are known
    as *neurons* . Each neuron (at least the ones most commonly used and the ones
    we use in this book) does a straightforward operation: it takes a certain number
    of inputs (real numbers) and calculates an output (also a real number). Remember
    that the inputs are indicated in this book with *x*[*i*] ∈ *ℝ* (real numbers)
    where *i* = 1, 2, …, *n*[*x*], *i* ∈ *ℕ* is an integer, and *n*[*x*] is the number
    of input attributes (often called features). As an example of input features,
    you can imagine the age and weight of a person (so we would have *n*[*x*] = 2).
    *x*[1] could be the age and *x*[2] could be the weight. In real life, the number
    of features can be easily very large (of the order of 10² − 10³ or higher).'
  id: totrans-5
  prefs: []
  type: TYPE_NORMAL
- en: 'There are several kinds of neurons that have been extensively studied. In this
    book we concentrate on the most commonly used one. The neuron we are interested
    in simply applies a function to a linear combination of all the inputs. In a more
    mathematical form, given *n*[*x*] real parameters *w*[*i*] ∈ *ℝ* (with *i* = 1,
    2, …, *n*[*x*]) and a constant *b* ∈ *ℝ* (usually called bias), the neuron will
    first calculate what is traditionally indicated in literature with *z*:'
  id: totrans-6
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ z={w}_1{x}_1+{w}_2{x}_2+\dots +{w}_{n_x}{x}_{n_x}+b $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equa.png)'
  id: totrans-7
  prefs: []
  type: TYPE_IMG
- en: It will then apply a function *f* (normally non-linear) to *z*, giving the output
    ![$$ \hat{y} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq1.png)
  id: totrans-8
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ \hat{y}=f(z)=f\left({w}_1{x}_1+{w}_2{x}_2+\dots +{w}_{n_x}{x}_{n_x}+b\right)
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equb.png)'
  id: totrans-9
  prefs: []
  type: TYPE_IMG
- en: Note
  id: totrans-10
  prefs: []
  type: TYPE_NORMAL
- en: 'Practitioners generally use the following terminology: *w*[*i*] are called
    *weights*, *b* is the *bias*, *x*[*i*] are the *input features****,*** and *f*
    is the *activation function*.'
  id: totrans-11
  prefs: []
  type: TYPE_NORMAL
- en: Let’s summarize the neuron computational steps again.
  id: totrans-12
  prefs: []
  type: TYPE_NORMAL
- en: Linearly combine all inputs *x*[*i*], calculating ![$$ z={w}_1{x}_1+{w}_2{x}_2+\dots
    +{w}_{n_x}{x}_{n_x}+b $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq2.png).
  id: totrans-13
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: Apply *f* to *z* giving the output ![$$ \hat{y}=f(z)=f\left({w}_1{x}_1+{w}_2{x}_2+\dots
    +{w}_{n_x}{x}_{n_x}+b\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq3.png).
  id: totrans-14
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: In the literature, you can find numerous representations for neurons. Figure
    [2-1](#Fig1) shows a graphical representation of the mathematical operations we
    just discussed to obtain the output ![$$ \hat{y} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq4.png)
    from the inputs.
  id: totrans-15
  prefs: []
  type: TYPE_NORMAL
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig1_HTML.jpg)'
  id: totrans-16
  prefs: []
  type: TYPE_IMG
- en: Figure 2-1
  id: totrans-17
  prefs: []
  type: TYPE_NORMAL
- en: A representation of a single neuron with the operation highlighted. This is
    also called the computational graph of a single neuron, or in other words, a graphical
    representation of the operations needed to calculate ![$$ \hat{y} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq5.png)
    from the inputs
  id: totrans-18
  prefs: []
  type: TYPE_NORMAL
- en: 'Figure [2-1](#Fig1) must be interpreted in this way:'
  id: totrans-19
  prefs: []
  type: TYPE_NORMAL
- en: The inputs are not placed in a bubble, simply to distinguish them from nodes
    that perform an actual calculation.
  id: totrans-20
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: The weight’s names are typically not written. The expected behavior is that
    before passing the inputs to the central bubble (or node), the inputs will be
    multiplied by the relative weight. The first input *x*[1] will be multiplied by
    *w*[1], *x*[2] by *w*[2], and so on.
  id: totrans-21
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: The first bubble (or node) will sum the inputs multiplied by the weights (the
    *x*[*i*]*w*[*i*] for *i* = 1, 2, …, *n*[*x*]) and then sum the result to the bias
    *b*.
  id: totrans-22
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: The last bubble (or node) will finally apply to the resulting value the activation
    function *f*.
  id: totrans-23
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: All the neurons we deal with in this book have exactly this structure. Very
    often an even simpler representation is used, as in Figure [2-2](#Fig2). In such
    a case, unless otherwise stated, it’s understood that the output is as follows
  id: totrans-24
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ \hat{y}=f(z)=f\left({w}_1{x}_1+{w}_2{x}_2+\dots +{w}_{n_x}{x}_{n_x}+b\right)
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equc.png)'
  id: totrans-25
  prefs: []
  type: TYPE_IMG
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig2_HTML.png)'
  id: totrans-26
  prefs: []
  type: TYPE_IMG
- en: Figure 2-2
  id: totrans-27
  prefs: []
  type: TYPE_NORMAL
- en: A simplified version of Figure [2-1](#Fig1). Unless otherwise stated, it is
    usually understood that the output is ![$$ \hat{y}=f(z)=f\left({w}_1{x}_1+{w}_2{x}_2+\dots
    +{w}_{n_x}{x}_{n_x}+b\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq6.png).
    The weights are often not explicitly reported in the neuron representation
  id: totrans-28
  prefs: []
  type: TYPE_NORMAL
- en: A Short Introduction to Matrix Notation
  id: totrans-29
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: When dealing with big datasets, the number of features is typically large (*n*[*x*]
    will be large) and so it is better to use a vector notation for the features and
    weights. The inputs can be indicated with
  id: totrans-30
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ \boldsymbol{x}=\left(\begin{array}{c}{x}_1\\ {}\vdots \\ {}{x}_{n_x}\end{array}\right)
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equd.png)'
  id: totrans-31
  prefs: []
  type: TYPE_IMG
- en: where we have indicated the vector with a bold-faced ***x***. For the weights,
    we use the same notation
  id: totrans-32
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ \boldsymbol{w}=\left(\begin{array}{c}{w}_1\\ {}\vdots \\ {}{w}_{n_x}\end{array}\right)
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Eque.png)'
  id: totrans-33
  prefs: []
  type: TYPE_IMG
- en: For consistency with formulas that we will use later, to multiply ***x*** and
    ***w***, we use matrix multiplication notation and therefore we write
  id: totrans-34
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ {\boldsymbol{w}}^T\boldsymbol{x}=\left({w}_1\dots {w}_{n_x}\right)\left(\begin{array}{c}{x}_1\\
    {}\vdots \\ {}{x}_{n_x}\end{array}\right)={w}_1{x}_1+{w}_2{x}_2+\dots +{w}_{n_x}{x}_{n_x}
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equf.png)'
  id: totrans-35
  prefs: []
  type: TYPE_IMG
- en: Where ***w***^(*T*) indicates the transpose of ***w***. *z* can then be written
    with this vector notation as
  id: totrans-36
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ z={\boldsymbol{w}}^T\boldsymbol{x}+b $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equg.png)'
  id: totrans-37
  prefs: []
  type: TYPE_IMG
- en: and the neuron output ![$$ \hat{y} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq7.png)
    as
  id: totrans-38
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ \hat{y}=f(z)=f\left({\boldsymbol{w}}^T\boldsymbol{x}+b\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equh.png)'
  id: totrans-39
  prefs: []
  type: TYPE_IMG
- en: 'Let’s now summarize the different components that define this neuron and the
    notation we use in this book:'
  id: totrans-40
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ \hat{y} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq8.png):
    Neuron (and later network) output'
  id: totrans-41
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '*f*(*z*): Activation function (sometimes called a transfer function) applied
    to z'
  id: totrans-42
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '***w***: Weights (vector with *n*[*x*] components)'
  id: totrans-43
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '*b*: Bias'
  id: totrans-44
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: An Overview of the Most Common Activation Functions
  id: totrans-45
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: There are many activation functions at your disposal to change the output of
    a neuron. Remember, an activation function is simply a mathematical function that
    transforms *z* in the output ![$$ \hat{y} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq9.png).
    Let’s look at the most common ones.
  id: totrans-46
  prefs: []
  type: TYPE_NORMAL
- en: Identity Function
  id: totrans-47
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
- en: This is the most basic function that you can use. It’s typically indicated with
    *I*(*z*). It merely returns the input value, unchanged (see Figure [2-3](#Fig3)).
    Mathematically we can write it as
  id: totrans-48
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ f(z)=I(z)=z $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equi.png)'
  id: totrans-49
  prefs: []
  type: TYPE_IMG
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig3_HTML.jpg)'
  id: totrans-50
  prefs: []
  type: TYPE_IMG
  zh: '![图2-4](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig3_HTML.jpg)'
- en: Figure 2-3
  id: totrans-51
  prefs: []
  type: TYPE_NORMAL
  zh: 图2-3
- en: The identity function
  id: totrans-52
  prefs: []
  type: TYPE_NORMAL
  zh: 标识函数
- en: 'This simple function will come in handy when discussing linear regression with
    one neuron later in the chapter. Implementing it^([2](#Fn2)) in Python with NumPy
    is incredibly trivial:'
  id: totrans-53
  prefs: []
  type: TYPE_NORMAL
  zh: 当本章后面讨论具有一个神经元的线性回归时，这个简单的函数将很有用。使用NumPy在Python中实现它^([2](#Fn2))非常简单：
- en: '[PRE0]'
  id: totrans-54
  prefs: []
  type: TYPE_PRE
  zh: '[PRE0]'
- en: Sigmoid Function
  id: totrans-55
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
  zh: Sigmoid函数
- en: This is a very commonly used function that gives only values between 0 and 1\.
    It is usually indicated with *σ*(*z*)
  id: totrans-56
  prefs: []
  type: TYPE_NORMAL
  zh: 这是一个非常常用的函数，它只给出介于0和1之间的值。它通常用*σ*(*z*)表示
- en: '![$$ f(z)=\sigma (z)=\frac{1}{1+{e}^{-z}} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equj.png)'
  id: totrans-57
  prefs: []
  type: TYPE_IMG
  zh: '![$$ f(z)=\sigma (z)=\frac{1}{1+{e}^{-z}} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equj.png)'
- en: It is primarily used for classification models, where we want to predict the
    probability as an output (remember that a probability may only assume values between
    0 and 1).
  id: totrans-58
  prefs: []
  type: TYPE_NORMAL
  zh: 它主要用于分类模型，其中我们希望预测输出为概率（记住，概率只能取介于0和1之间的值）。
- en: The calculation can be written in this form using NumPy functions
  id: totrans-59
  prefs: []
  type: TYPE_NORMAL
  zh: 使用NumPy函数，计算可以写成这种形式
- en: '[PRE1]'
  id: totrans-60
  prefs: []
  type: TYPE_PRE
  zh: '[PRE1]'
- en: 'Note It is very useful to know that if we have two NumPy arrays, `A` and `B`,
    the following are equivalent: `A/B` is equivalent to `np.divide(A,B)`, `A+B` is
    equivalent to `np.add(A,B)`, `A-B` is equivalent to `np.subtract(A,B)`, and `A*B`
    is equivalent to `np.multiply(A,B)`. If you know object-oriented programming,
    we say that in NumPy basic operations like `/`, `*`, `+` and `-` are *overloaded*.
    Note also that these four basic operations in NumPy act element-by-element (also
    called element-wise).'
  id: totrans-61
  prefs: []
  type: TYPE_NORMAL
  zh: '注意：知道如果我们有两个NumPy数组，`A`和`B`，以下操作是等价的：`A/B`等价于`np.divide(A,B)`，`A+B`等价于`np.add(A,B)`，`A-B`等价于`np.subtract(A,B)`，`A*B`等价于`np.multiply(A,B)`。如果你了解面向对象编程，我们可以说在NumPy中，基本操作如`/`、`*`、`+`和`-`是*重载的*。注意，NumPy中的这四个基本操作是按元素进行的（也称为逐元素）。 '
- en: We can write the sigmoid function in a more readable (at least for humans) form
    as
  id: totrans-62
  prefs: []
  type: TYPE_NORMAL
  zh: 我们可以将Sigmoid函数写成更易读的形式（至少对人类来说是这样）
- en: '[PRE2]'
  id: totrans-63
  prefs: []
  type: TYPE_PRE
  zh: '[PRE2]'
- en: As stated, `1.0 + np.exp(-z)` is equivalent to `np.add(1.0, np.exp(-z))` and
    `1.0 / (np.add(1.0, np.exp(-z)))` to `np.divide(1.0, np.add(1.0, np.exp(-z)))`.
    We want to draw your attention to another point in the formula. `np.exp(-z)` will
    have the dimensions of `z` (usually a vector that will have a length equal to
    the number of observations), while `1.0` is a scalar (a one-dimensional entity).
    How can Python sum the two? What happens is called *broadcasting* . Python, subject
    to certain constraints, “broadcasts” the smaller array (in this case, the `1.0`)
    across the larger one so that, at the end, the two have the same dimensions. In
    this case, the `1.0` becomes an array of the same dimensions of `z`, all filled
    with `1.0`. This is an important concept to understand, as it is very useful.
    You do not have to transform numbers in arrays, for example. Python will take
    care of this process for you. The rules on how broadcasting works in other cases
    are rather complex and go beyond this book’s scope. However, it is important to
    know that Python is doing something in the background.
  id: totrans-64
  prefs: []
  type: TYPE_NORMAL
  zh: 如前所述，`1.0 + np.exp(-z)`等价于`np.add(1.0, np.exp(-z))`，而`1.0 / (np.add(1.0, np.exp(-z)))`等价于`np.divide(1.0,
    np.add(1.0, np.exp(-z)))`。我们想提醒您注意公式中的另一个点。`np.exp(-z)`将具有`z`的维度（通常是一个长度等于观测数数量的向量），而`1.0`是一个标量（一个一维实体）。Python如何将这两个数相加？这被称为*广播*。Python在满足某些约束的条件下，“广播”较小的数组（在这种情况下，是`1.0`）到较大的数组上，使得最终两个数具有相同的维度。在这种情况下，`1.0`变成了一个与`z`相同维度的数组，全部填充了`1.0`。这是一个重要的概念，因为它非常有用。你不需要转换数组中的数字，例如。Python会为你处理这个过程。广播在其他情况下的规则相当复杂，超出了本书的范围。然而，重要的是要知道Python在后台做了某些操作。
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig4_HTML.jpg)'
  id: totrans-65
  prefs: []
  type: TYPE_IMG
  zh: '![图2-4](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig4_HTML.jpg)'
- en: Figure 2-4
  id: totrans-66
  prefs: []
  type: TYPE_NORMAL
  zh: 图2-4
- en: The sigmoid activation function is a s-shaped function that goes from 0 to 1
  id: totrans-67
  prefs: []
  type: TYPE_NORMAL
  zh: Sigmoid激活函数是一个从0到1的S形函数
- en: The sigmoid activation function (that you can see in Figure [2-4](#Fig4)) is
    especially used for models where we must predict the probability as an output,
    as logistic regression (remember that a probability may only assume values between
    0 and 1). Note that in Python, if *z* is big enough, it can happen that the function
    returns exactly 0 or 1 (depending on the sign of *z*) for rounding errors. In
    classification problems we will calculate log*σ*(*z*) or log(1 − *σ*(*z*)) very
    often, and therefore this can be a source of errors in Python since it will try
    to calculate log0, which is not defined. For example, you can start seeing `nan`
    appearing while calculating the cost function (more on that later).
  id: totrans-68
  prefs: []
  type: TYPE_NORMAL
- en: Note
  id: totrans-69
  prefs: []
  type: TYPE_NORMAL
- en: Although *σ*(*z* ) should never be exactly 0 or 1, while programming in Python
    the reality can be quite different. It may happen that, due to a very big *z*
    (positive or negative), Python will round the results to exactly 0 or 1\. This
    may give you errors while calculating the cost function for classification, since
    you need to calculate log*σ*(*z* ) and log(1 − *σ*(*z* )). Therefore, Python will
    try to calculate log0, which is not defined. This may happen, for example, if
    you don’t correctly normalize the input data or if you don’t correctly initialize
    the weights. For the moment, it’s important to remember that although everything
    seems under control mathematically, the reality while programming can be more
    difficult. It’s good to keep this in mind while debugging models that give, for
    example, `nan` as a result for the cost function.
  id: totrans-70
  prefs: []
  type: TYPE_NORMAL
- en: Tanh (Hyperbolic Tangent) Activation Function
  id: totrans-71
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
- en: The hyperbolic tangent is also an s-shaped curve that goes from -1 to 1, as
    shown in Figure [2-5](#Fig5).
  id: totrans-72
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ f(z)=\tanh (z)\#(1.7a) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equk.png)'
  id: totrans-73
  prefs: []
  type: TYPE_IMG
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig5_HTML.jpg)'
  id: totrans-74
  prefs: []
  type: TYPE_IMG
- en: Figure 2-5
  id: totrans-75
  prefs: []
  type: TYPE_NORMAL
- en: The tanh (or hyperbolic function) is an s-shaped curve that goes from -1 to
    1
  id: totrans-76
  prefs: []
  type: TYPE_NORMAL
- en: In Python, this can be easily implemented
  id: totrans-77
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE3]'
  id: totrans-78
  prefs: []
  type: TYPE_PRE
  zh: '[PRE3]'
- en: ReLU (Rectified Linear Unit) Activation Function
  id: totrans-79
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
- en: 'The ReLU has the following formula (see Figure [2-6](#Fig6)):'
  id: totrans-80
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ f(z)=\max \left(0,z\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equl.png)'
  id: totrans-81
  prefs: []
  type: TYPE_IMG
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig6_HTML.jpg)'
  id: totrans-82
  prefs: []
  type: TYPE_IMG
- en: Figure 2-6
  id: totrans-83
  prefs: []
  type: TYPE_NORMAL
- en: The ReLU function
  id: totrans-84
  prefs: []
  type: TYPE_NORMAL
- en: It is worth it to spend a few moments to see how to implement ReLU in a smart
    way in Python. Note that when you start using TensorFlow, it is implemented for
    you, but it’s still very instructive to see how different Python implementations
    can make a difference when implementing complex deep learning models.
  id: totrans-85
  prefs: []
  type: TYPE_NORMAL
- en: 'In Python you can implement the ReLU function in several ways. Here are four
    different ways (try to understand why they work before going on):'
  id: totrans-86
  prefs: []
  type: TYPE_NORMAL
- en: '`np.maximum(x, 0, x)`'
  id: totrans-87
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '`np.maximum(x, 0)`'
  id: totrans-88
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '`x * (x > 0)`'
  id: totrans-89
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: '`(abs(x) + x) / 2`'
  id: totrans-90
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: 'The four methods have very different execution speeds. Let’s generate a NumPy
    array with 10⁸ elements:'
  id: totrans-91
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE4]'
  id: totrans-92
  prefs: []
  type: TYPE_PRE
  zh: '[PRE4]'
- en: Now we measure the time needed by the four different versions of the ReLU function
    when applied to it. Let the following code run
  id: totrans-93
  prefs: []
  type: TYPE_NORMAL
  zh: 现在我们测量四个不同版本的ReLU函数在应用时的所需时间。让以下代码运行
- en: '[PRE5]'
  id: totrans-94
  prefs: []
  type: TYPE_PRE
  zh: '[PRE5]'
- en: The results are
  id: totrans-95
  prefs: []
  type: TYPE_NORMAL
  zh: 结果是
- en: '[PRE6]'
  id: totrans-96
  prefs: []
  type: TYPE_PRE
  zh: '[PRE6]'
- en: The difference is stunning . Method 1 is four times faster than method 4\. The
    `numpy` library is highly optimized, with many routines written in C. But knowing
    how to code efficiently still makes a difference and can have a great impact.
    Why is `np.maximum(x, 0, x)` faster than `np.maximum(x, 0)`? The first version
    updates `x` in place, without creating a new array. This can save a lot of time,
    especially when the arrays are big. If you don’t want to (or can’t) update the
    input vector in place, you can still use the `np.maximum(x, 0) version.`
  id: totrans-97
  prefs: []
  type: TYPE_NORMAL
  zh: 差异令人震惊。方法1比方法4快四倍。`numpy`库高度优化，其中许多例程是用C语言编写的。但知道如何高效编码仍然有区别，并且可以产生巨大影响。为什么`np.maximum(x,
    0, x)`比`np.maximum(x, 0)`更快？第一个版本在原地更新`x`，而不创建新的数组。这可以节省大量时间，尤其是在数组很大时。如果你不想（或不能）原地更新输入向量，你仍然可以使用`np.maximum(x,
    0)`版本。
- en: An implementation could look like this
  id: totrans-98
  prefs: []
  type: TYPE_NORMAL
  zh: 一种可能的实现方式如下
- en: '[PRE7]'
  id: totrans-99
  prefs: []
  type: TYPE_PRE
  zh: '[PRE7]'
- en: Note
  id: totrans-100
  prefs: []
  type: TYPE_NORMAL
  zh: 注意
- en: Remember that, when optimizing your code, even small changes can make a huge
    difference. In deep learning programs, the same chunk of code is repeated millions
    and billions of times, so even a small improvement can have a huge impact in the
    long run. Spending time optimizing your code is a necessary step that will pay
    off.
  id: totrans-101
  prefs: []
  type: TYPE_NORMAL
  zh: 记住，在优化你的代码时，即使是微小的变化也能产生巨大的影响。在深度学习程序中，相同的代码块会重复数百万甚至数十亿次，所以即使是小的改进在长期内也能产生巨大的影响。花时间优化你的代码是一个必要的步骤，这将带来回报。
- en: Leaky ReLU
  id: totrans-102
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
  zh: Leaky ReLU
- en: The Leaky ReLU (also known as Parametric Rectified Linear Unit) is given by
    the formula
  id: totrans-103
  prefs: []
  type: TYPE_NORMAL
  zh: Leaky ReLU（也称为参数化修正线性单元）由以下公式给出
- en: '![$$ f(z)=\left\{\begin{array}{ccc}\alpha z&amp; for&amp; z&lt;0\\ {}z&amp;
    for&amp; z\ge 0\end{array}\right. $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equm.png)'
  id: totrans-104
  prefs: []
  type: TYPE_IMG
  zh: '![公式](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equm.png)'
- en: with *α* a parameter typically of the order of 0.01\. See Figure [2-7](#Fig7).
  id: totrans-105
  prefs: []
  type: TYPE_NORMAL
  zh: 其中*α*是一个通常为0.01量级的参数。见图[2-7](#Fig7)。
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig7_HTML.jpg)'
  id: totrans-106
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig7_HTML.jpg)'
- en: Figure 2-7
  id: totrans-107
  prefs: []
  type: TYPE_NORMAL
  zh: 图2-7
- en: The Leaky ReLU activation function with *α* = 0.05\. This value has been chosen
    to make the difference between *x* > 0 and *x* < 0 more marked. Smaller values
    for *α* are typically used. Testing your model is required to find the best value
  id: totrans-108
  prefs: []
  type: TYPE_NORMAL
  zh: Leaky ReLU激活函数，*α* = 0.05。这个值被选择以使*x* > 0和*x* < 0之间的差异更加明显。通常使用较小的*α*值。测试你的模型以找到最佳值
- en: In Python, this can be implemented if the `relu(z)` function has been defined
    as
  id: totrans-109
  prefs: []
  type: TYPE_NORMAL
  zh: 在Python中，如果`relu(z)`函数已经被定义为
- en: '[PRE8]'
  id: totrans-110
  prefs: []
  type: TYPE_PRE
  zh: '[PRE8]'
- en: The Swish Activation Function
  id: totrans-111
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
  zh: Swish激活函数
- en: Ramachandran , Zopf, and Le from Google Brain [4] recently studied a new activation
    function that shows great promise in the deep learning world; they named it Swish.
    It is defined as
  id: totrans-112
  prefs: []
  type: TYPE_NORMAL
  zh: 来自Google Brain的Ramachandran、Zopf和Le最近研究了深度学习世界中具有巨大潜力的一个新激活函数；他们将其命名为Swish。它被定义为
- en: '![$$ f(z)= z\sigma \left(\beta z\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equn.png)'
  id: totrans-113
  prefs: []
  type: TYPE_IMG
  zh: '![公式](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equn.png)'
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig8_HTML.jpg)'
  id: totrans-114
  prefs: []
  type: TYPE_IMG
  zh: '![图片](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig8_HTML.jpg)'
- en: Figure 2-8
  id: totrans-115
  prefs: []
  type: TYPE_NORMAL
  zh: 图2-8
- en: The Swish activation function for three different values of the parameter *β*
  id: totrans-116
  prefs: []
  type: TYPE_NORMAL
  zh: Swish激活函数针对三个不同的参数*β*值
- en: where *β* is a learnable parameter (see Figure [2-8](#Fig8)). Their studies
    have shown that simply replacing ReLU activation functions with Swish improves
    classification accuracy on ImageNet by 0.9%, which in today’s deep learning world
    is a lot. ImageNet is a large database of images that is often used to benchmark
    new network architectures or algorithms, as in this case, networks with a different
    activation functions. You can find more information about ImageNet at [`http://www.image-net.org/`](http://www.image-net.org/).
  id: totrans-117
  prefs: []
  type: TYPE_NORMAL
  zh: 其中*β*是一个可学习的参数（见图[2-8](#Fig8)）。他们的研究表明，仅仅用Swish激活函数替换ReLU可以提高ImageNet的分类准确率0.9%，这在今天的深度学习世界中是一个很大的提升。ImageNet是一个大型图像数据库，常用于评估新的网络架构或算法，例如本例中的具有不同激活函数的网络。你可以在[`http://www.image-net.org/`](http://www.image-net.org/)找到更多关于ImageNet的信息。
- en: Other Activation Functions
  id: totrans-118
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
  zh: 其他激活函数
- en: There are many other activation functions, but those are rarely used. As a reference,
    here are some additional ones. The list is by no means comprehensive and should
    serve the purposes of giving you an idea of the variety of activation functions
    that can be used when developing neural networks.
  id: totrans-119
  prefs: []
  type: TYPE_NORMAL
- en: ArcTan
  id: totrans-120
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '![$$ f(z)={\tan}^{-1}z $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equo.png)'
  id: totrans-121
  prefs:
  - PREF_IND
  type: TYPE_IMG
- en: Exponential Linear unit (ELU)
  id: totrans-122
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '![$$ f(z)=\left\{\begin{array}{ccc}\alpha \left({e}^z-1\right)&amp; for&amp;
    z&lt;0\\ {}z&amp; for&amp; z\ge 0\end{array}\right. $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equp.png)'
  id: totrans-123
  prefs:
  - PREF_IND
  type: TYPE_IMG
- en: Softplus
  id: totrans-124
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: '![$$ f(z)=\ln \left(1+{e}^z\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equq.png)'
  id: totrans-125
  prefs:
  - PREF_IND
  type: TYPE_IMG
- en: '**Note** Practitioners almost always use only two activation functions: the
    sigmoid and the ReLU (with probably the ReLU in the lead). With both you can achieve
    good results. Both can, given a complex enough network architecture, approximate
    any nonlinear function.^([3](#Fn3)) Remember that when using TensorFlow, you do
    not have to implement them by yourself. TensorFlow offers an efficient implementation
    for you to use. But is important to know how each activation function behaves
    to understand when to use which one.'
  id: totrans-126
  prefs:
  - PREF_IND
  type: TYPE_NORMAL
- en: Now that we briefly discussed all the necessary components, you can use the
    neuron on some real problems. Let’s first see how to implement a neuron in Keras
    and then how to perform linear and logistic regression with it.
  id: totrans-127
  prefs: []
  type: TYPE_NORMAL
- en: How to Implement a Neuron in Keras
  id: totrans-128
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: Building a network with a single neuron in Keras is straightforward and can
    be done with
  id: totrans-129
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE9]'
  id: totrans-130
  prefs: []
  type: TYPE_PRE
  zh: '[PRE9]'
- en: The **Sequential** class groups a linear stack of layers into a `tf.keras.Model`.
    In this straightforward case, we need just one layer made by a single neuron,
    defined by the **layers.Dense** command, which specifies **1** unit (neuron) inside
    a layer and the shape of the input dataset. The `Dense` class implements densely
    connected neural networks’ layers (more on that in the next chapters).
  id: totrans-131
  prefs: []
  type: TYPE_NORMAL
- en: In the next paragraphs, you see two practical examples of how to use this simple
    approach—choosing the right activation function and the proper loss function—to
    solve two different problems, namely linear regression and logistic regression.
  id: totrans-132
  prefs: []
  type: TYPE_NORMAL
- en: 'Python Implementation Tips: Loops and NumPy'
  id: totrans-133
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: As you have just seen, Keras does all the dirty work for you. Of course, you
    can also implement the neuron from scratch, using Python standard functionalities
    such as lists and loops, but those tend to become very slow as the number of variables
    and observations grows. A good rule of thumb is to avoid loops when possible and
    use NumPy (or TensorFlow) methods as often as possible.
  id: totrans-134
  prefs: []
  type: TYPE_NORMAL
- en: 'It is easy to understand how fast NumPy can be (and how slow loops are). Let’s
    start by creating two standard lists of random numbers in Python with 10⁷ elements
    in each:'
  id: totrans-135
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE10]'
  id: totrans-136
  prefs: []
  type: TYPE_PRE
  zh: '[PRE10]'
- en: The actual values are not relevant for our purposes. We are simply interested
    in how fast Python can multiply two lists, element by element. The time reported
    was measured on a 2017 Microsoft Surface laptop and will vary greatly depending
    on the hardware that the code runs on. We are not interested in the absolute values,
    but only in how much faster NumPy is in comparison to standard Python loops. If
    you are using a Jupyter Notebook, it is useful to know how to time Python code
    in a cell. To do this, you can use a “magic command.” Those commands start (in
    a Jupyter Notebook) with `%%` or with `%`. It’s a good idea to check the official
    documentation to better understand how they work ([`http://ipython.readthedocs.io/en/stable/interactive/magics.html`](http://ipython.readthedocs.io/en/stable/interactive/magics.html)).
  id: totrans-137
  prefs: []
  type: TYPE_NORMAL
  zh: 实际值对我们来说并不重要。我们只是对Python如何逐个元素乘以两个列表的速度感兴趣。报告的时间是在2017年Microsoft Surface笔记本电脑上测量的，并且会根据代码运行的硬件有很大差异。我们感兴趣的并不是绝对值，而是NumPy与标准Python循环相比快了多少。如果你使用Jupyter
    Notebook，了解如何在单元格中计时Python代码是有用的。为此，你可以使用一个“魔法命令”。这些命令（在Jupyter Notebook中）以`%%`或`%`开头。了解它们的工作原理，查看官方文档是个好主意（[`http://ipython.readthedocs.io/en/stable/interactive/magics.html`](http://ipython.readthedocs.io/en/stable/interactive/magics.html))。
- en: Coming back to the test, let’s measure how much time a standard laptop takes
    to multiply, element by element, the two lists with standard loops. Using the
    code
  id: totrans-138
  prefs: []
  type: TYPE_NORMAL
  zh: 回到测试，让我们测量一个标准笔记本电脑使用标准循环逐个元素乘以两个列表需要多少时间。使用以下代码
- en: '[PRE11]'
  id: totrans-139
  prefs: []
  type: TYPE_PRE
  zh: '[PRE11]'
- en: 'gives us the following result (note that on your computer, you will probably
    get different numbers):'
  id: totrans-140
  prefs: []
  type: TYPE_NORMAL
  zh: 给出了以下结果（注意，在你的电脑上，你可能会得到不同的数字）：
- en: '[PRE12]'
  id: totrans-141
  prefs: []
  type: TYPE_PRE
  zh: '[PRE12]'
- en: The code needed roughly two seconds averaged over seven runs. Now let’s try
    to do the same multiplication, but this time using NumPy
  id: totrans-142
  prefs: []
  type: TYPE_NORMAL
  zh: 代码在七次运行中的平均时间大约是两秒。现在让我们尝试同样的乘法，但这次使用NumPy
- en: '[PRE13]'
  id: totrans-143
  prefs: []
  type: TYPE_PRE
  zh: '[PRE13]'
- en: We first converted the two lists to `numpy` arrays with the following code
  id: totrans-144
  prefs: []
  type: TYPE_NORMAL
  zh: 我们首先使用以下代码将两个列表转换为`numpy`数组
- en: '[PRE14]'
  id: totrans-145
  prefs: []
  type: TYPE_PRE
  zh: '[PRE14]'
- en: This time we get the following results
  id: totrans-146
  prefs: []
  type: TYPE_NORMAL
  zh: 这次我们得到了以下结果
- en: '[PRE15]'
  id: totrans-147
  prefs: []
  type: TYPE_PRE
  zh: '[PRE15]'
- en: 'The NumPy code needed only 21ms or, in other words, it was roughly 100 times
    faster than the code with standard loops. NumPy is faster for two reasons: the
    underlying routines are written in C, and it uses vectorized code as much as possible
    to speed up calculations on a large amount of data.'
  id: totrans-148
  prefs: []
  type: TYPE_NORMAL
  zh: NumPy代码只需要21毫秒，换句话说，它比使用标准循环的代码快了大约100倍。NumPy之所以更快，有两个原因：底层例程是用C编写的，并且尽可能多地使用向量化代码来加速大量数据的计算。
- en: Note
  id: totrans-149
  prefs: []
  type: TYPE_NORMAL
  zh: 注意
- en: Vectorized code refers to operations performed on multiple components of a vector
    (or a matrix) at the same time (in one statement). Passing matrixes to NumPy functions
    is an excellent example of *vectorized code*. NumPy will perform operations on
    big chunks of data simultaneously, obtaining much better performance than standard
    Python loops since the latter must operate on one element at a time. Note that
    part of the excellent performance NumPy shows is also due to the underlying routines
    being written in C.
  id: totrans-150
  prefs: []
  type: TYPE_NORMAL
  zh: 向量化代码指的是同时对向量的多个组件（或矩阵）执行操作（在一个语句中）。将矩阵传递给NumPy函数是向量化代码的一个优秀例子。NumPy将对大量数据同时执行操作，由于后者必须逐个元素操作，因此性能比标准的Python循环要好得多。请注意，NumPy显示的出色性能部分也归因于底层例程是用C编写的。
- en: While training deep learning models, you will find yourself doing this kind
    of operation over and over. Such a speed gain will make the difference between
    having a model that can be trained and one that will never give you a result.
  id: totrans-151
  prefs: []
  type: TYPE_NORMAL
  zh: 在训练深度学习模型时，你会发现自己在反复进行这类操作。这种速度提升将决定模型是否能够训练，以及模型是否永远不会给出结果。
- en: Linear Regression with a Single Neuron
  id: totrans-152
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 单神经元线性回归
- en: This section explains how to build your first model in Keras and how to use
    it to solve a basic statistical problem. You can, of course, perform linear regression
    quickly by applying traditional math formulas or using dedicated functions such
    as those found in Scikit-learn. For example, you can find the complete implementation
    of linear regression from scratch with NumPy, using the analytical formulas (see
    [`http://adl.toelt.ai/single_neuron/Linear_regression_with_numpy.html`](http://adl.toelt.ai/single_neuron/Linear_regression_with_numpy.html))
    in the online version of the book. However, it is instructive to follow this example,
    since it gives a practical understanding of how the building blocks of deep learning
    architectures (i.e., neurons) work.
  id: totrans-153
  prefs: []
  type: TYPE_NORMAL
  zh: 本节解释了如何在Keras中构建您的第一个模型以及如何使用它来解决一个基本的统计问题。当然，您可以通过应用传统的数学公式或使用如Scikit-learn中的专用函数快速执行线性回归。例如，您可以在书籍的在线版本中找到使用NumPy从头开始实现线性回归的完整实现（使用解析公式，见[http://adl.toelt.ai/single_neuron/Linear_regression_with_numpy.html](http://adl.toelt.ai/single_neuron/Linear_regression_with_numpy.html)）。然而，遵循这个例子是有教育意义的，因为它提供了对深度学习架构构建块（即神经元）如何工作的实际理解。
- en: 'If you remember, we have said many times that NumPy is highly optimized to
    perform several parallel operations simultaneously. To get the best performance
    possible, it’s essential to write your equations in matrix form and feed the matrixes
    to NumPy. In this way, the code will be as efficient as possible. Remember: avoid
    loops at all costs whenever possible.'
  id: totrans-154
  prefs: []
  type: TYPE_NORMAL
  zh: 如果您还记得，我们多次说过NumPy高度优化以同时执行多个并行操作。为了获得最佳性能，将您的方程式写成矩阵形式并将矩阵输入到NumPy中是至关重要的。这样，代码将尽可能高效。请记住：尽可能避免使用循环。
- en: The Dataset for the Real-World Example
  id: totrans-155
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 实际案例的数据集
- en: 'To make things a bit more interesting, let’s use an instructive dataset. We
    will use the so-called radon dataset [3]. Radon is a radioactive gas that enters
    homes through contact points with the ground. It is a carcinogen that is the primary
    cause of lung cancer in non-smokers. Radon levels vary significantly from household
    to household. The dataset contains measured radon levels in U.S. homes by county
    and state. The `activity` label is the measured radon concentration in pCi/L (referred
    to as the *target* variable, i.e., the one we want to predict using a linear regression
    model). Important features are:'
  id: totrans-156
  prefs: []
  type: TYPE_NORMAL
  zh: 为了让事情更有趣，让我们使用一个有教育意义的数据库集。我们将使用所谓的氡数据库[3]。氡是一种放射性气体，通过与地面的接触点进入房屋。它是一种致癌物质，是非吸烟者肺癌的主要原因。氡水平在家庭之间差异很大。该数据集包含按县和州测量的美国家庭氡水平。`activity`标签是测量的氡浓度，以pCi/L为单位（称为*目标*变量，即我们想要使用线性回归模型预测的变量）。重要的特征包括：
- en: Floor (the floor of the house in which the measurement was taken)
  id: totrans-157
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 楼层（测量时所在的房屋楼层）
- en: County (the U.S. county in which the house is located)
  id: totrans-158
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 县（房屋所在的美国县）
- en: uppm (a measurement of the uranium level of the soil by county)
  id: totrans-159
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: uppm（通过县测量的土壤铀水平）
- en: This dataset fits a classical regression problem well since it contains a continuous
    variable (radon activity) to predict. The model that we will build is made of
    one neuron and will fit a linear function to the different features.
  id: totrans-160
  prefs: []
  type: TYPE_NORMAL
  zh: 由于该数据集包含一个用于预测的连续变量（氡活性），因此它非常适合经典回归问题。我们将构建的模型由一个神经元组成，并将拟合一个线性函数到不同的特征。
- en: You do not need to understand or study the features. The goal here is to understand
    how to build a linear regression model with what you have learned. Generally,
    in a machine learning project, you would first study your input data, check its
    distribution, quality, missing values, and so on. But we skip this part to concentrate
    on the implementation with Keras.
  id: totrans-161
  prefs: []
  type: TYPE_NORMAL
  zh: 您不需要理解或研究这些特征。这里的目的是理解如何使用您所学到的知识构建线性回归模型。通常，在一个机器学习项目中，您首先研究您的输入数据，检查其分布、质量、缺失值等。但我们跳过这部分，以便集中精力使用Keras进行实现。
- en: Note
  id: totrans-162
  prefs: []
  type: TYPE_NORMAL
  zh: 注意
- en: In machine learning, the variable we want to predict is usually called the *target
    variable*.
  id: totrans-163
  prefs: []
  type: TYPE_NORMAL
  zh: 在机器学习中，我们想要预测的变量通常被称为*目标变量*。
- en: Now, let’s look at the data. We will skip all the import and load details for
    simplicity and concentrate on the fundamental steps of the code, such as dataset
    preparation, model creation, and performance evaluation. Remember that you can
    find the complete code on the online version of the book. We start by checking
    how many observations we have
  id: totrans-164
  prefs: []
  type: TYPE_NORMAL
  zh: 现在，让我们看看数据。为了简化，我们将跳过所有导入和加载的细节，并专注于代码的基本步骤，例如数据集准备、模型创建和性能评估。请记住，您可以在书籍的在线版本中找到完整的代码。我们首先检查我们有多少观测值
- en: '[PRE16]'
  id: totrans-165
  prefs: []
  type: TYPE_PRE
  zh: '[PRE16]'
- en: The code will give the following results
  id: totrans-166
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE17]'
  id: totrans-167
  prefs: []
  type: TYPE_PRE
  zh: '[PRE17]'
- en: So, we have 919 different measurements of radon activity in 85 distinct counties.
    The `radon_features.head()` command will give the following table as output (the
    first five lines of the `pandas` dataframe)
  id: totrans-168
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE18]'
  id: totrans-169
  prefs: []
  type: TYPE_PRE
  zh: '[PRE18]'
- en: We have four features (`floor`, `county`, `log_uranium_ppm`, and `pcterr`) that
    we will use as predictors of radon activity.
  id: totrans-170
  prefs: []
  type: TYPE_NORMAL
- en: As suggested, we have prepared the data in matrix form. Let’s briefly review
    the notation, which will come in handy when building the neuron. Normally we have
    many observations (919 in this case). We use an upper index to indicate the different
    observations between parentheses. The *i*^(*th*) observation is indicated with
    ***x***^((*i*)), and the *j*^(*th*) feature of the *i*^(*th*) observation is indicated
    with ![$$ {\boldsymbol{x}}_j^{(i)} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq10.png).
    We indicate the number of observations with *m*.
  id: totrans-171
  prefs: []
  type: TYPE_NORMAL
- en: Note
  id: totrans-172
  prefs: []
  type: TYPE_NORMAL
- en: In this book, *m* is the *number of observations* and *n*[*x*] is the *number
    of features****.*** Our *j*^(*th*) feature of the *i*^(*th*) observation is indicated
    with ![$$ {\boldsymbol{x}}_j^{(i)} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_IEq11.png).
    In deep learning projects, the bigger the *m*, the better. So be prepared to deal
    with a massive number of observations.
  id: totrans-173
  prefs: []
  type: TYPE_NORMAL
- en: '*n*[*x*] in this example is equal to 4, while *m* is equal to 919\. The entire
    set of inputs (features and observations) can be therefore written using the following
    notation'
  id: totrans-174
  prefs: []
  type: TYPE_NORMAL
- en: '![$$ X=\left(\begin{array}{ccc}{x}_1^{(1)}&amp; \dots &amp; {x}_4^{(1)}\\ {}\vdots
    &amp; \ddots &amp; \vdots \\ {}{x}_1^{(m)}&amp; \dots &amp; {x}_4^{(m)}\end{array}\right)
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equr.png)'
  id: totrans-175
  prefs: []
  type: TYPE_IMG
- en: where each row is an observation, and each column represents a feature in the
    matrix *X* that has the dimensions *m* × 4.
  id: totrans-176
  prefs: []
  type: TYPE_NORMAL
- en: Dataset Splitting
  id: totrans-177
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
- en: 'In any machine learning project, to check how the model generalizes to unseen
    data, you need to split the dataset into different subsets.^([4](#Fn4)) When you
    build a machine learning model, you first need to train the model, and then you
    have to test it (i.e., verify the model’s performances on novel data). The most
    common way to do this is to split the dataset into two subsets: 80% of the original
    dataset to train the model (the more data you have, the better your model will
    perform) and the remaining 20% to test it.^([5](#Fn5))'
  id: totrans-178
  prefs: []
  type: TYPE_NORMAL
- en: 'The following code splits the dataset randomly into two parts with the following
    proportions: 80%/20%.'
  id: totrans-179
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE19]'
  id: totrans-180
  prefs: []
  type: TYPE_PRE
  zh: '[PRE19]'
- en: This code will give as output the following lines
  id: totrans-181
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE20]'
  id: totrans-182
  prefs: []
  type: TYPE_PRE
  zh: '[PRE20]'
- en: We will use 733 observations to train our linear regression model, and we will
    then evaluate it on the remaining 186 observations.
  id: totrans-183
  prefs: []
  type: TYPE_NORMAL
- en: Linear Regression Model
  id: totrans-184
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: Keep in mind that using a single-neuron model is overkill for a linear regression
    task. We could solve linear regression exactly without using gradient descent
    or a similar optimization algorithm. As mentioned, you can find an exact regression
    solution example, implemented with NumPy library, in the book’s online version.
  id: totrans-185
  prefs: []
  type: TYPE_NORMAL
  zh: 请记住，对于线性回归任务来说，使用单个神经元模型是过度的。我们可以不使用梯度下降或类似的优化算法，精确地解决线性回归问题。正如之前提到的，你可以在本书的在线版本中找到一个精确的回归解决方案示例，该示例使用NumPy库实现。
- en: 'Given that the dataset can be expressed as a matrix (*X*) and the label we
    want to predict as a column vector (*y*), when we employ one neuron to perform
    linear regression, we simply try to find the best weights (*W*) that appear in
    the following equation:'
  id: totrans-186
  prefs: []
  type: TYPE_NORMAL
  zh: 由于数据集可以表示为一个矩阵（*X*），而我们想要预测的标签是一个列向量（*y*），当我们使用一个神经元进行线性回归时，我们只需尝试找到以下方程中出现的最佳权重（*W*）：
- en: '![$$ \hat{y}= WX+b $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equs.png)'
  id: totrans-187
  prefs: []
  type: TYPE_IMG
  zh: '![预测公式](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equs.png)'
- en: The weights and bias need to be chosen so that that the network output is as
    similar as possible to the expected target variable.
  id: totrans-188
  prefs: []
  type: TYPE_NORMAL
  zh: 需要选择权重和偏置，使得网络输出尽可能接近预期的目标变量。
- en: If you remember how a neuron is structured, you can easily see that, for the
    neuron to give as output a linear combination of the inputs, we need to use the
    *identity activation function* *.* How can we measure how close the neuron’s outputs
    are to the target variables? The difference is measured by using the Mean Squared
    Error (MSE) function.
  id: totrans-189
  prefs: []
  type: TYPE_NORMAL
  zh: 如果你记得神经元的结构，你就可以很容易地看出，为了使神经元输出输入的线性组合，我们需要使用**恒等激活函数**。我们如何衡量神经元的输出与目标变量之间的接近程度呢？这个差异是通过使用均方误差（MSE）函数来衡量的。
- en: '![$$ L\left(\boldsymbol{w},b\right)=\frac{1}{m}\sum \limits_{i=1}^m{\left({y}^{(i)}-{\boldsymbol{w}}^T{\boldsymbol{x}}^{(i)}-b\right)}^2
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equt.png)'
  id: totrans-190
  prefs: []
  type: TYPE_IMG
  zh: '![损失函数公式](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equt.png)'
- en: where the sum is over all *m* observations. This is the typical loss function
    chosen in a regression problem. By minimizing *L*(***w***, *b*) with respect to
    the weights and bias, we can find their optimal values.
  id: totrans-191
  prefs: []
  type: TYPE_NORMAL
  zh: 其中求和是对所有*m*个观测值进行的。这是在回归问题中选择的典型损失函数。通过相对于权重和偏置最小化*L*(***w***, *b*)，我们可以找到它们的最佳值。
- en: Minimizing *L*(***w***, *b*) is done with an optimizer. If you remember from
    the last chapter, gradient descent is the most basic example of an optimizer and
    could be used to solve this problem. Since is not available out of the box in
    TensorFlow, we use the RMSProp optimizer for practical reasons. Don’t worry if
    you don’t know how it works. Just know that it is simply a more intelligent version
    of the GD algorithm. You learn how it works in detail in the following chapters.
  id: totrans-192
  prefs: []
  type: TYPE_NORMAL
  zh: 最小化*L*(***w***, *b*)是通过一个优化器来完成的。如果你还记得上一章的内容，梯度下降是最基本的优化器示例，可以用来解决这个问题。由于TensorFlow中默认没有提供，我们出于实际考虑使用了RMSProp优化器。如果你不知道它是如何工作的，不必担心。只需知道，它只是GD算法的一个更智能的版本。你将在接下来的章节中详细了解它是如何工作的。
- en: Keras Implementation
  id: totrans-193
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: Keras实现
- en: If you have no experience with Keras , you can consult the appendixes of this
    book. There you will find an introduction to Keras that will give you enough information
    to be able to understand the following discussion.
  id: totrans-194
  prefs: []
  type: TYPE_NORMAL
  zh: 如果你没有Keras的经验，可以查阅本书的附录。在那里，你将找到一个关于Keras的介绍，这将为你提供足够的信息，以便理解接下来的讨论。
- en: Implementing what we discussed with Keras is straightforward. The following
    function builds the one-neuron model for linear regression.
  id: totrans-195
  prefs: []
  type: TYPE_NORMAL
  zh: 使用Keras实现我们讨论的内容是直接的。以下函数构建了用于线性回归的单神经元模型。
- en: '[PRE21]'
  id: totrans-196
  prefs: []
  type: TYPE_PRE
  zh: '[PRE21]'
- en: 'Let’s analyze what this code does:'
  id: totrans-197
  prefs: []
  type: TYPE_NORMAL
  zh: 让我们分析一下这段代码的功能：
- en: First of all, we defined the network structure (also called network architecture)
    with the `keras.Sequential` class, adding one layer^([6](#Fn6)) made of one neuron
    (`layers.Dense`) and with `input dimensions` equal to the number of features used
    to build the model. The activation function is the one set by default, i.e. the
    identity function.
  id: totrans-198
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: 首先，我们使用`keras.Sequential`类定义了网络结构（也称为网络架构），添加了一个由一个神经元（`layers.Dense`）组成的一层，并且`输入维度`等于构建模型所使用的特征数量。激活函数是默认设置的，即恒等函数。
- en: Then, we defined the optimizer (`tf.keras.optimizers.RMSprop`), setting the
    learning rate to 0.001\. The optimizer is the algorithm that Keras will use to
    minimize the cost function. We use the RMSprop algorithm in this example.
  id: totrans-199
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: Finally, we compile the model (i.e., we configure the model for training), setting
    its loss function (i.e., the cost function to be minimized), its optimizer, and
    the metric to be calculated during performance evaluation (`model.compile`). The
    function returns the built `model` as a single Python object.
  id: totrans-200
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
- en: The *learning rate* is a very important parameter for the optimizer. In fact,
    it strongly influences the convergence of the minimization process. It is a common
    and good behavior to try different learning rate values and observe how the model’s
    convergence changes.
  id: totrans-201
  prefs: []
  type: TYPE_NORMAL
- en: Now, let’s apply the `build_model` function and look at the model summary
  id: totrans-202
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE22]'
  id: totrans-203
  prefs: []
  type: TYPE_PRE
  zh: '[PRE22]'
- en: This code gives the following output
  id: totrans-204
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE23]'
  id: totrans-205
  prefs: []
  type: TYPE_PRE
  zh: '[PRE23]'
- en: Thus, we have five parameters to be trained—the weights associated with the
    four features, plus the bias .
  id: totrans-206
  prefs: []
  type: TYPE_NORMAL
- en: The Model’s Learning Phase
  id: totrans-207
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: Training our neuron means finding the weights and biases that minimize the chosen
    cost function (the MSE in this case). The *minimization process* is iterative;
    therefore, it is necessary to decide when to stop it. For this example, we simply
    set a fixed number of epochs. We train the model for 1000 epochs and then look
    at the results in terms of the MSE.
  id: totrans-208
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE24]'
  id: totrans-209
  prefs: []
  type: TYPE_PRE
  zh: '[PRE24]'
- en: As you can see, training the model in Keras is straightforward. It is enough
    to apply the `fit` method to our model object. `fit` takes as inputs the training
    data and the number of epochs.
  id: totrans-210
  prefs: []
  type: TYPE_NORMAL
- en: The cost function clearly decreases for a while and then stays almost constant.
    That is a good sign, indicating that the cost function has reached a minimum.
    That does not mean that our model is good or that it will give good predictions.
    This tells us only that learning has somehow worked. A very good way to immediately
    visualize the loss function decrease is by plotting the cost function vs. the
    number of epochs. This can be seen in Figure [2-9](#Fig9).
  id: totrans-211
  prefs: []
  type: TYPE_NORMAL
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig9_HTML.jpg)'
  id: totrans-212
  prefs: []
  type: TYPE_IMG
- en: Figure 2-9
  id: totrans-213
  prefs: []
  type: TYPE_NORMAL
- en: The cost function behavior during the model training, applied to the radon dataset
    with a learning rate of 0.001
  id: totrans-214
  prefs: []
  type: TYPE_NORMAL
- en: Looking at Figure [2-9](#Fig9), you can see that, after 400 epochs, the cost
    function remains almost constant in its value, indicating that a minimum has been
    reached.
  id: totrans-215
  prefs: []
  type: TYPE_NORMAL
- en: Since we are doing a linear regression, we are interested in the coefficients
    of the linear equation. Those are the weights of our neuron. The first four are
    the linear regression coefficients, while the last one is the bias term. You can
    compare these numbers with the ones obtained by performing linear regression with
    traditional math formulas and using the NumPy library in the online version of
    the book. To get the weights in Keras, you simply use the `get_weights()` call.
  id: totrans-216
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE25]'
  id: totrans-217
  prefs: []
  type: TYPE_PRE
  zh: '[PRE25]'
- en: which returns
  id: totrans-218
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE26]'
  id: totrans-219
  prefs: []
  type: TYPE_PRE
  zh: '[PRE26]'
- en: Those are the weights we were expecting.
  id: totrans-220
  prefs: []
  type: TYPE_NORMAL
- en: Model’s Performance Evaluation on Unseen Data
  id: totrans-221
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: To determine if the model you just trained is suited for unseen data, you must
    check its performance over a dataset containing only unseen data (the test set).
    Then, predicted radon activity values are compared with real values (`test_y`)
    by simply plotting predictions vs. true values, as shown in Figure [2-10](#Fig10).
    A perfect model will show points distributed over the black line. The less precise
    the predictions are, the more spread out the points will be around the diagonal.
  id: totrans-222
  prefs: []
  type: TYPE_NORMAL
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig10_HTML.jpg)'
  id: totrans-223
  prefs: []
  type: TYPE_IMG
- en: Figure 2-10
  id: totrans-224
  prefs: []
  type: TYPE_NORMAL
- en: The predicted target value vs. measured target value for our model, applied
    to our testing data
  id: totrans-225
  prefs: []
  type: TYPE_NORMAL
- en: If you have followed so far, congratulations! You just built your very first
    neural network, with just one neuron, but still a neural network!
  id: totrans-226
  prefs: []
  type: TYPE_NORMAL
- en: Logistic Regression with a Single Neuron
  id: totrans-227
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
- en: 'Now let’s try to solve a classification problem with a single neuron. Logistic
    regression is a classical algorithm and probably the simplest classification.
    We will consider a binary classification problem: that means we will deal with
    the problem of recognizing two classes (that we label as 0 or 1). We need an activation
    function that’s different from the one we used for linear regression, a different
    cost function to minimize, and a slight modification of the output of our neuron.
    The goal is to be able to build a model that can predict if a certain new observation
    is in one of two classes. The neuron should give as output the probability *P*(*y* = 1|
    *x*) of the input *x* to be of class 1\. We will then classify our observation
    as being in class 1 if *P*(*y* = 1| *x*) > 0.5 or in class 0 if *P*(*y* = 1| *x*) < 0.5.'
  id: totrans-228
  prefs: []
  type: TYPE_NORMAL
- en: It is instructive to compare this example with the one about linear regression,
    since they both are applications of the one-neuron model yet are used to solve
    different tasks. You should pay attention to the similarities and differences
    with the linear regression model discussed previously. You are going to see how
    simple using Keras is in this case and how, by changing a few things (such as
    the activation and loss functions), you can easily obtain a different model that
    can solve a different problem.
  id: totrans-229
  prefs: []
  type: TYPE_NORMAL
- en: The Dataset for the Classification Problem
  id: totrans-230
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: As in the linear regression example, we use a dataset taken from the real world,
    to make things more interesting. We employ the BCCD dataset, a small-scale dataset
    for blood cell classification. The dataset can be downloaded from its GitHub repository
    [8]. For this dataset, two Python scripts have been developed to make preparing
    the data easier. All the code can be found in the online version of the book.
    In the example, a slightly modified version of the two scripts is used. Remember,
    you are not interested at this point in how the data looks or how it is cleaned.
    You should focus on how to build the model with Keras.
  id: totrans-231
  prefs: []
  type: TYPE_NORMAL
- en: 'The dataset contains three kinds of labels:'
  id: totrans-232
  prefs: []
  type: TYPE_NORMAL
- en: Red blood cells (RBC)
  id: totrans-233
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: White blood cells (WBC)
  id: totrans-234
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: Platelets
  id: totrans-235
  prefs:
  - PREF_OL
  type: TYPE_NORMAL
- en: To make it a binary classification problem, we will consider only the RBC and
    WBC types. The model that will be trained has one neuron and will predict if an
    image contains an RBC or a WBC type by using the `xmin`, `xmax`, `ymin`, and `ymax`
    variables as features.
  id: totrans-236
  prefs: []
  type: TYPE_NORMAL
- en: For simplicity, as in the linear regression example, we will skip all the import
    and load details and concentrate on the fundamental steps of the code, such as
    dataset preparation, model creation, and performance evaluation. You can find
    the complete code on the online version of the book. Note that the greatest differences
    between this case and the linear regression lie in the chosen activation and cost
    function.
  id: totrans-237
  prefs: []
  type: TYPE_NORMAL
- en: 'Here is the data:'
  id: totrans-238
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE27]'
  id: totrans-239
  prefs: []
  type: TYPE_PRE
  zh: '[PRE27]'
- en: This code returns
  id: totrans-240
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE28]'
  id: totrans-241
  prefs: []
  type: TYPE_PRE
  zh: '[PRE28]'
- en: Let’s display the first lines of the data
  id: totrans-242
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE29]'
  id: totrans-243
  prefs: []
  type: TYPE_PRE
  zh: '[PRE29]'
- en: which prints to the screen
  id: totrans-244
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE30]'
  id: totrans-245
  prefs: []
  type: TYPE_PRE
  zh: '[PRE30]'
- en: The dataset is made up of 4527 observations, one target column (`cell_type`),
    and four features (`xmin`, `xmax`, `ymin`, and `ymax`). When working with images,
    it is always useful to get an idea of how they look. You can see an example in
    Figure [2-11](#Fig11).
  id: totrans-246
  prefs: []
  type: TYPE_NORMAL
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig11_HTML.jpg)'
  id: totrans-247
  prefs: []
  type: TYPE_IMG
- en: Figure 2-11
  id: totrans-248
  prefs: []
  type: TYPE_NORMAL
- en: A sample image from the BCCD dataset
  id: totrans-249
  prefs: []
  type: TYPE_NORMAL
- en: Note that the features we employ in this example are not the pixel values of
    the image, but the location of the edges of the bounding box of the cell. In fact,
    for each image, we have only four values (`xmin`, `xmax`, `ymin`, and `ymax`).
  id: totrans-250
  prefs: []
  type: TYPE_NORMAL
- en: Dataset Splitting
  id: totrans-251
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: As stated, in any machine learning project, you have to split the dataset in
    different subsets. Let’s create a `train` and a `test` dataset by splitting the
    dataset randomly into two parts with a ratio of 80/20, as we did with the radon
    dataset.
  id: totrans-252
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE31]'
  id: totrans-253
  prefs: []
  type: TYPE_PRE
  zh: '[PRE31]'
- en: This code will give as output the following lines
  id: totrans-254
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE32]'
  id: totrans-255
  prefs: []
  type: TYPE_PRE
  zh: '[PRE32]'
- en: So, we use 3631 observations to train our logistic regression model and then
    evaluate it on the remaining 896 observations.
  id: totrans-256
  prefs: []
  type: TYPE_NORMAL
- en: Now comes a particularly important point. The labels in this dataset as imported
    will be `'WBC'` or `'RBC'` strings (they simply tell you if an image contains
    white or red blood cells). But we will build our cost function with the assumptions
    that our class labels are 0 and 1\. That means we need to change our `train_y`
    and `test_y` arrays.
  id: totrans-257
  prefs: []
  type: TYPE_NORMAL
- en: Note
  id: totrans-258
  prefs: []
  type: TYPE_NORMAL
- en: When doing binary classifications, remember to check the values of the labels
    you are using for training. Sometimes using the wrong labels (not 0 and 1) may
    cost you quite some time in understanding why the model is not learning.
  id: totrans-259
  prefs: []
  type: TYPE_NORMAL
- en: '[PRE33]'
  id: totrans-260
  prefs: []
  type: TYPE_PRE
  zh: '[PRE33]'
- en: Now all images containing RBC will have a label of `0`, and all images containing
    WBC will have a label of `1`.
  id: totrans-261
  prefs: []
  type: TYPE_NORMAL
- en: The Logistic Regression Model
  id: totrans-262
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
- en: This model will be made up of one neuron and its goal will be to recognize two
    classes (labeled `0` or `1`, referring to RBC or WBC inside a cell image). This
    is a *binary classification problem* *.*
  id: totrans-263
  prefs: []
  type: TYPE_NORMAL
- en: As opposed to linear regression, the activation function will be a *sigmoid
    function* (leading to a different neuron's output) and the cost function will
    be *cross-entropy* [7]. If you don’t remember what a sigmoid function is, reread
    the beginning of this chapter. We use it because we want our neuron to output
    the probability of our observation being in class 1\. Therefore, we need an activation
    function that can assume only values between 0 and 1; otherwise we cannot regard
    it as a probability. The cross-entropy for one observation is
  id: totrans-264
  prefs: []
  type: TYPE_NORMAL
  zh: 与线性回归不同，激活函数将是一个**sigmoid函数**（导致不同神经元的输出），而损失函数将是**交叉熵**[7]。如果你不记得sigmoid函数是什么，请重新阅读本章的开头部分。我们使用它是因为我们希望我们的神经元输出观察结果属于类别1的概率。因此，我们需要一个只能取0到1之间值的激活函数；否则，我们不能将其视为概率。一个观察值的交叉熵为
- en: '![$$ L\left({\hat{y}}^{(i)},{y}^{(i)}\right)=-\left({y}^{(i)}\ \log {\hat{y}}^{(i)}+\left(1-{y}^{(i)}\right)\log
    \left(1-{\hat{y}}^{(i)}\right)\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equu.png)'
  id: totrans-265
  prefs: []
  type: TYPE_IMG
  zh: '![$$ L\left({\hat{y}}^{(i)},{y}^{(i)}\right)=-\left({y}^{(i)}\ \log {\hat{y}}^{(i)}+\left(1-{y}^{(i)}\right)\log
    \left(1-{\hat{y}}^{(i)}\right)\right) $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equu.png)'
- en: In presence of more than one observation, the cost function is the sum over
    all observations
  id: totrans-266
  prefs: []
  type: TYPE_NORMAL
  zh: 在存在多个观察值的情况下，损失函数是所有观察值的总和
- en: '![$$ L\left(\boldsymbol{w},b\right)=\frac{1}{m}\sum \limits_{i=1}^mL\left({\hat{y}}^{(i)},{y}^{(i)}\right)
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equv.png)'
  id: totrans-267
  prefs: []
  type: TYPE_IMG
  zh: '![$$ L\left(\boldsymbol{w},b\right)=\frac{1}{m}\sum \limits_{i=1}^mL\left({\hat{y}}^{(i)},{y}^{(i)}\right)
    $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equv.png)'
- en: Explaining the cross-entropy loss function goes beyond the scope of this book,
    but if you are interested, you can find it described in many books and websites.
    For example, you can check out [7].
  id: totrans-268
  prefs: []
  type: TYPE_NORMAL
  zh: 解释交叉熵损失函数超出了本书的范围，但如果你感兴趣，你可以在许多书籍和网站上找到它的描述。例如，你可以查看[7]。
- en: Keras Implementation
  id: totrans-269
  prefs:
  - PREF_H4
  type: TYPE_NORMAL
  zh: Keras实现
- en: The following function builds the one-neuron model for logistic regression.
    The implementation is remarkably similar to that of linear regression. The differences,
    as already mentioned, are the activation function, the cost function, and the
    metrics (accuracy in this case, which we analyze more in detail in the testing
    phase).
  id: totrans-270
  prefs: []
  type: TYPE_NORMAL
  zh: 以下函数构建了逻辑回归的单神经元模型。实现方式与线性回归非常相似。如前所述，区别在于激活函数、损失函数和度量标准（在这种情况下是准确率，我们将在测试阶段更详细地分析）。
- en: '[PRE34]'
  id: totrans-271
  prefs: []
  type: TYPE_PRE
  zh: '[PRE34]'
- en: Now, let’s apply the `build_model` function and look at the model summary
  id: totrans-272
  prefs: []
  type: TYPE_NORMAL
  zh: 现在，让我们应用`build_model`函数并查看模型摘要
- en: '[PRE35]'
  id: totrans-273
  prefs: []
  type: TYPE_PRE
  zh: '[PRE35]'
- en: This code gives the following output
  id: totrans-274
  prefs: []
  type: TYPE_NORMAL
  zh: 这段代码给出了以下输出
- en: '[PRE36]'
  id: totrans-275
  prefs: []
  type: TYPE_PRE
  zh: '[PRE36]'
- en: We have five parameters to be trained in this case as well—the weights associated
    with the four features plus the bias.
  id: totrans-276
  prefs: []
  type: TYPE_NORMAL
  zh: 在这种情况下，我们也有五个参数需要训练——与四个特征相关的权重以及偏差。
- en: The Model’s Learning Phase
  id: totrans-277
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 模型的学习阶段
- en: As with the linear regression example, training our neuron means finding the
    weights and biases that minimize the cost function. The cost function we chose
    to minimize in our logistic regression task is the cross-entropy function, as
    discussed in the previous section.
  id: totrans-278
  prefs: []
  type: TYPE_NORMAL
  zh: 与线性回归示例一样，训练我们的神经元意味着找到最小化损失函数的权重和偏差。在我们之前的逻辑回归任务中，我们选择最小化的损失函数是交叉熵函数，如前所述。
- en: We start training our model for 500 epochs and then look at the summary in terms
    of performances (accuracy).
  id: totrans-279
  prefs: []
  type: TYPE_NORMAL
  zh: 我们开始以500个epoch训练我们的模型，然后查看性能摘要（准确率）。
- en: '[PRE37]'
  id: totrans-280
  prefs: []
  type: TYPE_PRE
  zh: '[PRE37]'
- en: In Figure [2-12](#Fig12), you can see the cost function vs. the number of iterations
    associated with the learning phase.
  id: totrans-281
  prefs: []
  type: TYPE_NORMAL
  zh: 在图[2-12](#Fig12)中，你可以看到与学习阶段相关的损失函数与迭代次数的关系。
- en: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig12_HTML.jpg)'
  id: totrans-282
  prefs: []
  type: TYPE_IMG
  zh: '![](../images/463356_2_En_2_Chapter/463356_2_En_2_Fig12_HTML.jpg)'
- en: Figure 2-12
  id: totrans-283
  prefs: []
  type: TYPE_NORMAL
  zh: 图2-12
- en: The cost function resulting in our model applied to the BCCD dataset with a
    learning rate of 0.001
  id: totrans-284
  prefs: []
  type: TYPE_NORMAL
  zh: 损失函数导致我们的模型应用于BCCD数据集，学习率为0.001
- en: Note that, after 100 epochs, the cost function remains almost constant, indicating
    that a minimum has been reached.
  id: totrans-285
  prefs: []
  type: TYPE_NORMAL
  zh: 注意，在100个epoch之后，损失函数几乎保持不变，这表明已经达到了最小值。
- en: The Model’s Performance Evaluation
  id: totrans-286
  prefs:
  - PREF_H3
  type: TYPE_NORMAL
  zh: 模型的性能评估
- en: Now, to determine if the model you have just trained is suited for unseen data,
    you must check its performance against the test set. Moreover, an *optimizing
    metric* must be chosen. For a binary classification problem, a classic metric
    is the *accuracy,* which can be understood as a measure of how well the classifier
    correctly identified the two classes of the dataset. Mathematically it can be
    calculated as
  id: totrans-287
  prefs: []
  type: TYPE_NORMAL
  zh: 现在，为了确定你刚刚训练的模型是否适合未见过的数据，你必须检查其在测试集上的性能。此外，必须选择一个*优化指标*。对于二元分类问题，一个经典的指标是*准确率*，它可以理解为衡量分类器正确识别数据集两个类别的好坏程度。从数学上讲，它可以计算为
- en: '![$$ accuracy=\frac{number\ of\ cases\ correctly\ identified}{total\ number\
    of\ cases} $$](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equw.png)'
  id: totrans-288
  prefs: []
  type: TYPE_IMG
  zh: '![准确率 = 正确识别的案例数 / 总案例数](../images/463356_2_En_2_Chapter/463356_2_En_2_Chapter_TeX_Equw.png)'
- en: where the number of cases correctly identified is the sum of all positive samples
    and negative samples (i.e., all 0s and 1s) that were correctly classified. These
    are usually called true positives and true negatives.
  id: totrans-289
  prefs: []
  type: TYPE_NORMAL
  zh: 其中正确识别的案例数是所有正确分类的正样本和负样本（即所有0和1）的总和。这些通常被称为真阳性（true positives）和真阴性（true negatives）。
- en: To get the accuracy, we can run this code (remember, we will classify an observation
    *i* to be in class 0 if *P*(*y*^((*i*)) = 1| ***x***^((*i*))) < 0.5 or to be in
    class 1 if *P*(*y*^((*i*)) = 1| ***x***^((*i*))) > 0.5)
  id: totrans-290
  prefs: []
  type: TYPE_NORMAL
  zh: 为了获得准确率，我们可以运行此代码（记住，我们将把观察值*i*分类到类别0，如果*P*(y^(*i*)) = 1| ***x***^(*i*)) < 0.5，或者将其分类到类别1，如果*P*(y^(*i*))
    = 1| ***x***^(*i*)) > 0.5）。
- en: '[PRE38]'
  id: totrans-291
  prefs: []
  type: TYPE_PRE
  zh: '[PRE38]'
- en: This code prints this to the screen
  id: totrans-292
  prefs: []
  type: TYPE_NORMAL
  zh: 此代码将此输出到屏幕上
- en: '[PRE39]'
  id: totrans-293
  prefs: []
  type: TYPE_PRE
  zh: '[PRE39]'
- en: With this model, we reach an accuracy of 98%. Not bad for a network with just
    one neuron.
  id: totrans-294
  prefs: []
  type: TYPE_NORMAL
  zh: 使用这个模型，我们达到了98%的准确率。对于一个只有一个神经元的网络来说，这已经很不错了。
- en: Conclusion
  id: totrans-295
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 结论
- en: 'This chapter looked at many things. You learned how a neuron works and what
    its main components are. You also learned about the most common activation functions
    and saw how to implement a model with a single neuron in Keras to solve two problems:
    linear and logistic regression. In the next chapter, we look at how to build neural
    networks with a large number of neurons and how to train them.'
  id: totrans-296
  prefs: []
  type: TYPE_NORMAL
  zh: 本章探讨了众多内容。你学习了神经元的工作原理及其主要组成部分。你还了解了最常见的激活函数，并看到了如何在Keras中使用单个神经元实现模型来解决两个问题：线性回归和逻辑回归。在下一章中，我们将探讨如何构建具有大量神经元的神经网络以及如何训练它们。
- en: Note
  id: totrans-297
  prefs: []
  type: TYPE_NORMAL
  zh: 注意
- en: Linear and logistic regression are two classical statistical models that can
    be implemented in many ways. This chapter used the neural network language to
    implement them and to show you how flexible neural networks are. When you understand
    their inner components, you can use them in many ways.
  id: totrans-298
  prefs: []
  type: TYPE_NORMAL
  zh: 线性回归和逻辑回归是两种经典的统计模型，可以以多种方式实现。本章使用神经网络语言来实现它们，并展示了神经网络是多么灵活。当你理解它们的内部组件时，你可以以多种方式使用它们。
- en: Exercises
  id: totrans-299
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 练习
- en: 'Exercise 1 (Linear Regression) (Level: Easy)'
  id: totrans-300
  prefs: []
  type: TYPE_NORMAL
  zh: 练习 1（线性回归）（难度：简单）
- en: Try using only one feature to predict radon activity and see how the results
    change.
  id: totrans-301
  prefs: []
  type: TYPE_NORMAL
  zh: 尝试仅使用一个特征来预测氡活动，并观察结果如何变化。
- en: 'Exercise 2 (Linear Regression) (Level: Medium)'
  id: totrans-302
  prefs: []
  type: TYPE_NORMAL
  zh: 练习 2（线性回归）（难度：中等）
- en: Try to change the `learning_rate` parameter and then observe how the model’s
    convergence changes. Then try to reduce the `EPOCHS` parameter and observe when
    the model cannot reach convergence.
  id: totrans-303
  prefs: []
  type: TYPE_NORMAL
  zh: 尝试更改`learning_rate`参数，然后观察模型收敛性的变化。然后尝试减少`EPOCHS`参数，观察模型何时无法达到收敛。
- en: 'Exercise 3 (Linear Regression) (Level: Medium)'
  id: totrans-304
  prefs: []
  type: TYPE_NORMAL
  zh: 练习 3（线性回归）（难度：中等）
- en: Try to see how a model’s results change based on the training dataset’s size
    (reduce it and use different sizes, comparing the results).
  id: totrans-305
  prefs: []
  type: TYPE_NORMAL
  zh: 尝试观察模型结果如何根据训练数据集的大小变化（减小它并使用不同的大小，比较结果）。
- en: 'Exercise 4 (Logistic Regression) (Level: Medium)'
  id: totrans-306
  prefs: []
  type: TYPE_NORMAL
  zh: 练习 4（逻辑回归）（难度：中等）
- en: Try to change the `learning_rate` parameter and observe how the model’s convergence
    changes. Then try to reduce the `EPOCHS` parameter and observe when the model
    cannot reach convergence.
  id: totrans-307
  prefs: []
  type: TYPE_NORMAL
  zh: 尝试更改`learning_rate`参数，观察模型收敛性的变化。然后尝试减少`EPOCHS`参数，观察模型何时无法达到收敛。
- en: 'Exercise 5 (Logistic Regression) (Level: Medium)'
  id: totrans-308
  prefs: []
  type: TYPE_NORMAL
  zh: 练习 5（逻辑回归）（难度：中等）
- en: Try to see how a model’s results change based on the training dataset’s size
    (reduce it and use different sizes comparing the results).
  id: totrans-309
  prefs: []
  type: TYPE_NORMAL
  zh: 尝试观察模型结果如何根据训练数据集的大小变化（减小它并使用不同的大小，比较结果）。
- en: 'Exercise 6 (Logistic Regression) (Level: Difficult)'
  id: totrans-310
  prefs: []
  type: TYPE_NORMAL
  zh: 练习 6（逻辑回归）（难度：困难）
- en: Try to add to labels to `Platelets` samples and generalize the binary classification
    model to a multiclass one (three possible classes).
  id: totrans-311
  prefs: []
  type: TYPE_NORMAL
  zh: 尝试为“血小板”样本添加标签，并将二元分类模型推广到多类分类（三种可能的类别）。
- en: References
  id: totrans-312
  prefs:
  - PREF_H2
  type: TYPE_NORMAL
  zh: 参考文献
- en: '[1] [`https://spectrum.ieee.org/tech-talk/computing/software/biggest-neural-network-ever-pushes-ai-deep-learning`](https://spectrum.ieee.org/tech-talk/computing/software/biggest-neural-network-ever-pushes-ai-deep-learning),
    last accessed 27.12.2017.'
  id: totrans-313
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[1] [《最大的神经网络：推动人工智能深度学习》](https://spectrum.ieee.org/tech-talk/computing/software/biggest-neural-network-ever-pushes-ai-deep-learning),
    最后访问日期：2017年12月27日。'
- en: '[2] R. Rojas (1996), *Neural Networks: A Systematic Introduction,* Springer-Verlag
    Berlin Heidelberg.'
  id: totrans-314
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[2] R. Rojas (1996), *《神经网络：系统介绍》*, Springer-Verlag Berlin Heidelberg.'
- en: '[3] [`https://www.tensorflow.org/datasets/catalog/radon`](https://www.tensorflow.org/datasets/catalog/radon),
    last accessed 09.01.2021.'
  id: totrans-315
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[3] [《Radon数据集》](https://www.tensorflow.org/datasets/catalog/radon), 最后访问日期：2021年1月9日。'
- en: '[4] Lever, Jake, Martin Krzywinski, and Naomi Altman. “Points of significance:
    model selection and overfitting.” (2016): 703.'
  id: totrans-316
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[4] Lever, Jake, Martin Krzywinski, and Naomi Altman. “Points of significance:
    model selection and overfitting.” (2016): 703.'
- en: '[5] Srivastava, Nitish, et al. “Dropout: a simple way to prevent neural networks
    from overfitting.” The journal of machine learning research 15.1 (2014): 1929-1958.'
  id: totrans-317
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[5] Srivastava, Nitish, et al. “Dropout: a simple way to prevent neural networks
    from overfitting.” The journal of machine learning research 15.1 (2014): 1929-1958.'
- en: '[6] Bengio, Yoshua. “Practical recommendations for gradient-based training
    of deep architectures.” Neural networks: Tricks of the trade. Springer, Berlin,
    Heidelberg, 2012\. 437-478.'
  id: totrans-318
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[6] Bengio, Yoshua. “Practical recommendations for gradient-based training
    of deep architectures.” Neural networks: Tricks of the trade. Springer, Berlin,
    Heidelberg, 2012\. 437-478.'
- en: '[7] [`https://rdipietro.github.io/friendly-intro-to-cross-entropy-loss/`](https://rdipietro.github.io/friendly-intro-to-cross-entropy-loss/),
    last accessed 10.01.2021.'
  id: totrans-319
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[7] [《交叉熵损失的友好介绍》](https://rdipietro.github.io/friendly-intro-to-cross-entropy-loss/),
    最后访问日期：2021年1月10日。'
- en: '[8] [`https://www.tensorflow.org/datasets/catalog/bccd`](https://www.tensorflow.org/datasets/catalog/bccd),
    last accessed 10.01.2021.'
  id: totrans-320
  prefs:
  - PREF_UL
  type: TYPE_NORMAL
  zh: '[8] [《BCCD数据集》](https://www.tensorflow.org/datasets/catalog/bccd), 最后访问日期：2021年1月10日。'
