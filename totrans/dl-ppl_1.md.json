["```py\n>>> import sys\n>>> print (sys.version_info)\n```", "```py\n$> pip\n# or\n$> easy_install\n```", "```py\n>>>import numpy\n```", "```py\n>>> import numpy\nTraceback (most recent call last):\nFile \"\", line 1, in \nImportError: No module named numpy\n```", "```py\n$> pip install\n# or\n$> easy_install numpy\n```", "```py\n$> pip install ipython\n```", "```py\n$> ipython\n```", "```py\n$> pip install jupyter\n```", "```py\n$> jupyter notebook\n```", "```py\n        $ sudo apt-get install python3-pip python3-dev\n        $ sudo pip3 install -U virtualenv #system-wide install\n        ```", "```py\n        $ sudo easy_install pip\n        $ sudo pip install --upgrade virtualenv\n        ```", "```py\n        $ source ~/tensorflow/bin/activate\n        (tensorflow)$\n        ```", "```py\n        (tensorflow)$ pip install --upgrade https://storage.googleapis.com/tensorflow/linux/cpu/tensorflow-0.5.0-cp27-none-linux_x86_64.whl\n        ```", "```py\n        (tensorflow)$ pip install --upgrade https://storage.googleapis.com/tensorflow/mac/tensorflow-0.5.0-py2-none-any.whl\n        ```", "```py\n(tensorflow)$ deactivate\n```", "```py\nx = 1\ny = x + 9\nprint(y)\nimport tensorflow as tf\nx = tf.constant(1,name='x')\ny = tf.Variable(x+9,name='y')\nprint(y)\n```", "```py\n10\n\n```", "```py\nimport tensorflow as tf\n```", "```py\nx = tf.constant(1,name='x')\n```", "```py\ny = tf.Variable(x+9,name='y')\n```", "```py\nprint(y)\n```", "```py\n10\n```", "```py\nmodel = tf.global_variables_initializer()\n```", "```py\nwith tf.Session() as session:\nsession.run(model)\nprint(session.run(y))\n```", "```py\nimport tensorflow as tf\nsess = tf.Session()\ntens1 = tf.constant([[[1,2],[2,3]],[[3,4],[5,6]]])\nprint(sess.run(tens1)[1,1,0])\n# 5\n```", "```py\nfrom __future__ import print_function\nimport tensorflow as tf\n# The value returned by the constructor represents\n# the output of the Constant op.\nhello = tf.constant('Hello, TensorFlow!')\n# Start tf session\nsess = tf.Session()\n# Run the op\nprint(sess.run(hello))\n```", "```py\n# our first variable in the \"global_variable\" set\nvar = tf.Variable(0)\nadd_operation = tf.add(var, 1)\nupdate_operation = tf.assign(var, add_operation)\nwith tf.Session() as sess:\n# once define variables, you have to initialize\n# them by doing this\nsess.run(tf.global_variables_initializer())\nfor _ in range(3):\nsess.run(update_operation)\nprint(sess.run(var))\n```", "```py\nimport matplotlib.image as mp_image\nfilename = \"lena.jpeg\"\ninput_image = mp_image.imread(filename)\n```", "```py\nprint('input dim = {}'.format(input_image.ndim))\nprint('input shape = {}'.format(input_image.shape))\n# input dim = 3\n# input shape = (220, 220, 3)\n```", "```py\nimport matplotlib.pyplot as plt\nplt.imshow(input_image)\nplt.show()\n```", "```py\nimport tensorflow as tf\nmy_image = tf.placeholder(\"uint8\",[None,None,3])\n```", "```py\nslice = tf.slice(my_image,[10,0,0],[16,-1,-1])\n```", "```py\nwith tf.Session() as session:\nresult = session.run(slice,feed_dict={my_image: input_image})\nprint(result.shape)\nplt.imshow(result)\nplt.show()\n```", "```py\nimport tensorflow as tf\nx = tf.placeholder(tf.float32)\ny = 2*x*x*x\ngrad = tf.gradients(y, x)\nwith tf.Session() as session:\ngrad_val = session.run(grad,feed_dict={x:1})\nprint(grad_val)\n# [6.0]\n```", "```py\nimport matplotlib.pyplot as plt\nimport numpy as np\nimport tensorflow as tf\n```", "```py\nnp.random.seed(seed=47)\n# Linear space of 101 and [-1,1]\ntrX = np.linspace(-1, 1, 101)\n#Create The y function based on the x axis\ntrY = 2 * trX + np.random.randn(*trX.shape) * 0.4 + 0.2\n```", "```py\n# Create a new figure\nplt.figure()\n#Plot a scatter draw of the random data points\nplt.scatter(trX,trY)\n# Draw one line with the line function\nplt.plot (trX, .2 + 2 * trX)\nplt.show()\n```", "```py\n# create symbolic variables\nX = tf.placeholder(\"float\", name=\"X\")\ny = tf.placeholder(\"float\", name = \"y\")\n```", "```py\nwith tf.name_scope(\"Model\"):\ndef model(X, W, b):\n# We just define the line as X*w + b0\nreturn tf.add(tf.multiply(X,W), b)\n# create a shared variable\nw = tf.Variable(-1.0, name=\"b0\")\n# create a shared variable\nb = tf.Variable(-2.0, name=\"b1\")\ny_model = model(X, W, b)\n```", "```py\nwith tf.name_scope(\"CostFunction\"):\n# use sqr error for cost\ncost = (tf.pow(Y-y_model, 2))\n```", "```py\ntrain_op = tf.train.GradientDescentOptimizer(0.05)\n.minimize(cost)\n```", "```py\nsess = tf.Session()\ninit = tf.global_variables_initializer()\n# you can use you own path\ntf.train.write_graph(sess.graph, '/home/ubuntu/linear','graph.pbtxt')\ncost_op = tf.summary.scalar(\"loss\", cost)\nmerged = tf.summary.merge_all()\nsess.run(init)\nwriter = tf.summary.FileWriter('/home/ubuntu/linear', sess.graph)\n```", "```py\nfor i in range(100):\nfor (x, y) in zip(trX, trY):\nsess.run(train_op, feed_dict={X: x, Y: y})\nsummary_str = sess.run(cost_op, feed_dict={X: x, Y: y})\nwriter.add_summary(summary_str, i)\nb0temp=b.eval(session=sess)\nb1temp=w.eval(session=sess)\nplt.plot (trX, b0temp + b1temp * trX )\nplt.show()\n```", "```py\nprint (\"w = {}\".format(sess.run(w))) # Should be around 2\nprint (\"b = {}\".format(sess.run(b))) #Should be around 0.2\nOutput:\nw = 1.8842864\nb = 0.12578306\n```", "```py\nplt.scatter(trX,trY)\nplt.plot (trX, sess.run(b) + trX * sess.run(w))\nplt.show()\n```", "```py\nimport matplotlib.pyplot as plt\nimport numpy as np\nimport tensorflow as tf\n# Linear space of 101 and [-1,1]\ntrX = np.linspace(-1, 1, 101)\n#Create The y function based on the x axis\ntrY = 2 * trX + np.random.randn(*trX.shape) * 0.4 + 0.2\ntrX = np.linspace(-1, 1, 101)\ntrY = 2 * trX + np.random.randn(*trX.shape) * 0.4 + 0.2\n# create a y value which is approximately linear\n#  but with some random noise\nplt.figure() # Create a new figure\nplt.scatter(trX,trY) #Plot a scatter draw of\n# the random datapoints\n# Draw one line with the line function\nplt.plot (trX, .2 + 2 * trX)\nplt.show()\n# create symbolic variables\nX = tf.placeholder(\"float\", name=\"X\")\nY = tf.placeholder(\"float\", name = \"Y\")\nwith tf.name_scope(\"Model\"):\ndef model(X, w, b):\n# We just define the line as X*w + b0\nreturn tf.add(tf.multiply(X,w), b)\n# create a shared variable\nw = tf.Variable(-1.0, name=\"b0\")\n# create a shared variable\nb = tf.Variable(-2.0, name=\"b1\")\ny_model = model(X, w, b)\n# use sqr error for cost\nwith tf.name_scope(\"CostFunction\"):\ncost = (tf.pow(Y-y_model, 2))\ntrain_op = tf.train.GradientDescentOptimizer(0.05).minimize(cost)\nsess = tf.Session()\ninit = tf.global_variables_initializer()\ntf.train.write_graph(sess.graph, '/home/ubuntu/linear','graph.pbtxt')\ncost_op = tf.summary.scalar(\"loss\", cost)\nmerged = tf.summary.merge_all()\nsess.run(init)\nwriter = tf.summary.FileWriter('/home/ubuntu/linear', sess.graph)\nfor i in range(100):\nfor (x, y) in zip(trX, trY):\nsess.run(train_op, feed_dict={X: x, Y: y})\nsummary_str = sess.run(cost_op, feed_dict={X: x, Y: y})\nwriter.add_summary(summary_str, i)\nb0temp=b.eval(session=sess)\nb1temp=w.eval(session=sess)\nplt.plot (trX, b0temp + b1temp * trX )\nplt.show()\nprint (sess.run(w)) # Should be around 2\nprint (sess.run(b)) #Should be around 0.2\nplt.scatter(trX,trY)\nplt.plot (trX, sess.run(b) + trX * sess.run(w))\n```", "```py\nimport matplotlib.pyplot as plt\nimport numpy as np\nimport tensorflow as tf\n```", "```py\n# setting required X and Y values to\n# perform XOR operation\nX_train = [[0,0],[0,1],[1,0],[1,1]]\ny_train = [[0],[1],[1],[0]]\n```", "```py\n# create symbolic variables\nX = tf.placeholder(tf.float64, shape=[4, 2],name=\"X\")\ny = tf.placeholder(tf.float64, shape=[4, 1],name=\"Y\")\n```", "```py\n#number of training examples (Rows)\nm = np.shape(X)[0]\n#number of features (Columns)\nn = np.shape(X)[1]\n#number of nodes in the hidden layer\nhidden_s = 2\n#learning rate initialization\nl_r = 1\n```", "```py\nwith tf.name_scope(\"Model\"):\ndef model():\n# cast tensor to new type\n# and make new variable theta1, theta2\ntheta1 = tf.cast(tf.Variable(tf.random_normal([3,hidden_s]),name=\"theta1\"),tf.float64)\ntheta2=tf.cast(tf.Variable(tf.random_normal([hidden_s+1,1]),name = \"theta2\"),tf.float64)\n#conducting forward propagation\na1 = tf.concat([np.c_[np.ones(m)], X], 1)\n# the weights of the first layer are multiplied\n# by the input of the first layer\nz1 = tf.matmul(a1,theta1)\n# the input of the second layer is the output of\n# the first layer passed through\n# activation function and column of biases is added\na2 = tf.concat([np.c_[np.ones(m)],tf.sigmoid(z1)],1)\n# the input of the second layer is\n# multiplied by the weights\nz3 = tf.matmul(a2,theta2)\n# the output is passed through the activation\n# function to obtain the final probability\nh3 = tf.sigmoid(z3)\nreturn h3\n```", "```py\ny_model = model()\n```", "```py\nwith tf.name_scope(\"CostFunction\"):\ncost=\n-tf.reduce_sum(Y*tf.log(y_model)+(1-Y)*tf.log(1-y_model),axis=1)\n```", "```py\ntrain_op = tf.train.GradientDescentOptimizer(learning_rate=l_r)\n.minimize(cost)\n```", "```py\nsess = tf.Session()\ninit = tf.global_variables_initializer()\ntf.train.write_graph(sess.graph, '/home/ubuntu/xor','graph.pbtxt')\ncost_op = tf.summary.scalar(\"loss\", cost)\nmerged = tf.summary.merge_all()\nsess.run(init)\nwriter = tf.summary.FileWriter('/home/ubuntu/xor', sess.graph)\n```", "```py\nfor i in range(100):\nsess.run(train_op, feed_dict={X: X_train, Y: Y_train})\nif i%100==0:\nprint(\"Epoch:\",i)\nprint(\"Hyp:\",sess.run(y_model,feed_dict ={X:X_train,Y:Y_train}))\nEpoch: 0\nHyp: [[0.4708459 ]\n[0.50110425]\n[0.50382591]\n[0.51823803]]\n```", "```py\n# Should be around 2\nprint (\"w = {}\".format(sess.run(w)))\n#Should be around 0.2\nprint (\"b = {}\".format(sess.run(b)))\nOutput:\nw = 1.7057617\nb = 0.20965417\n```", "```py\nplt.scatter(trX,trY)\nplt.plot (trX, sess.run(b) + trX * sess.run(w))\n```", "```py\nimport matplotlib.pyplot as plt\nimport numpy as np\nimport tensorflow as tf\n#setting required X and Y values to perform XOR operation\nX_train = [[0,0],[0,1],[1,0],[1,1]]\nY_train = [[0],[1],[1],[0]]\n## ADD PLOT HERE\nplt.figure() # Create a new figure\nplt.scatter(X_train) #Plot a scatter draw of the random datapoints\n# Draw one line with the line function\nplt.show()\nX = tf.placeholder(tf.float64, shape=[4, 2], name=\"X\") # create symbolic variables\nY = tf.placeholder(tf.float64, shape=[4, 1], name = \"Y\")\nm = np.shape(X)[0] #number of training examples\nn = np.shape(X)[1] #number of features\nhidden_s = 2       #number of nodes in the hidden layer\nl_r = 1            #learning rate initialization\nwith tf.name_scope(\"Model\"):\ndef model():\ntheta1 = tf.cast(tf.Variable(tf.random_normal([3,hidden_s]),name = \"theta1\"),tf.float64)\ntheta2 = tf.cast(tf.Variable(tf.random_normal([hidden_s+1,1]), name = \"theta2\"),tf.float64)\n# conducting forward propagation\na1 = tf.concat([np.c_[np.ones(m)], X], 1)\n# the weights of the first layer are multiplied by the input of the first layer\nz1 = tf.matmul(a1,theta1)\n# the input of the second layer is the output of the first layer, passed through the activation function and column of biases is added\na2 = tf.concat([np.c_[np.ones(m)],tf.sigmoid(z1)],1)\n# the input of the second layer is multiplied by the weights\nz3 = tf.matmul(a2,theta2)\n# the output is passed through the activation function to obtain the final probability\nh3 = tf.sigmoid(z3)\nreturn h3\ny_model = model()\nwith tf.name_scope(\"CostFunction\"):\ncost = -tf.reduce_sum(Y*tf.log(y_model)+(1-Y)*tf.log(1-y_model),axis = 1)\ntrain_op = tf.train.GradientDescentOptimizer(learning_rate = l_r).minimize(cost)\nsess = tf.Session()\ninit = tf.global_variables_initializer()\ntf.train.write_graph(sess.graph, '/home/ubuntu/xor','graph.pbtxt')\ncost_op = tf.summary.scalar(\"loss\", cost)\nmerged = tf.summary.merge_all()\nsess.run(init)\nwriter = tf.summary.FileWriter('/home/ubuntu/xor', sess.graph)\nfor i in range(100):\nsess.run(train_op, feed_dict={X: X_train, Y: Y_train})\nif i%100==0:\nprint(\"Epoch:\",i)\nprint(\"Hyp:\",sess.run(y_model,feed_dict = {X:X_train,Y:Y_train}))\nprint (sess.run(w)) # Should be around 2\nprint (sess.run(b)) #Should be around 0.2\nplt.scatter(trX,trY)\nplt.plot (trX, sess.run(b) + trX * sess.run(w))\n```"]