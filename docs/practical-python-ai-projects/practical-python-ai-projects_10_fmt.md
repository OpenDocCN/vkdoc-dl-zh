## 索引

### A

#### 抽象方法
两栖动物共存水族馆通过执行模型建模语言 `MPSolver` OR-Tools 库，采用模型求解三步法来模拟捕食关系

#### 伴随理论

#### 装箱问题
约束条件、决策变量、可执行模型、权重数组、卡车容量限制、约束条件、决策变量、退化问题、最优包裹分配、最优包裹分配、对称性破缺、最优卡车装载量、对称性破缺、过度约束的包裹与卡车、包裹列表与重量、用于确定边界的简单启发式方法、卡车、对称性破缺约束 `symmetry_break` 参数、卡车编号、卡车选择变量、目标函数、包裹与卡车、重量容量变化

#### 资本预算
背包问题、目标函数、对称性破缺

#### 混合问题
与调和问题的对比、原油/粗汽油，参见 原油/粗汽油

### C

#### 连续性方程

#### 凸函数

#### 曲线拟合
抽象方法、数据与拟合曲线、变量倍增法、欧几里得距离不等式、最小二乘法、正规方程、最优解、多项式、假定函数、软约束

#### 下料问题
约束条件、消费卷、决策变量、可执行模型、边界计算、客户需求、决策变量、上下界、目标函数、最优解、模式搜索、后处理、对称性破缺约束、目标函数、纸张浪费、预分配、更优模式、列生成、不同模式 `DualValues`、线性规划、新模式、最优解、优化器、终止准则、处理器与求解器、产品卷

### D, E

#### 决策变量

#### 膳食问题
创建求解器实例、数据与求解启发式方法、最小成本膳食、OR-Tools 对象、产品组合问题、变体

#### 迪杰斯特拉算法

#### 离散线性规划
参见 整数规划 (`IP`)

### F

#### 设施选址问题
约束条件、大 M 法、成本、供需变量、决策变量、配送成本、可执行模型、目标函数、工厂建设成本、`Solar-1138`、变体

#### 弗洛伊德-沃舍尔算法

#### 全职员工 (`FTE`)

### G

#### 谷歌运筹学工具 (`OR-Tools`)

### H

#### 启发式方法

#### 超平面
单元格数据与分离、分类方向、方程、最大化间隔

### I

#### 指示变量

#### 整数规划 (`IP`)
装箱问题，参见 装箱问题、决策变量、描述、元素、指示变量、整数变量、伪整数模型、集合覆盖问题、集合打包问题、`TSP`，参见 旅行商问题 (`TSP`)

### J, K

#### 作业车间调度
约束条件、决策变量、描述、可执行模型、图形化表示、机器与持续时间、机器、目标变量、变体

### L

#### 老虎与淑女

#### 最小二乘法

#### 线性连续模型
元素、目标函数、产品组合问题、项目管理，参见 项目管理

### M

#### 最大流问题
将工人分配到工作、流量守恒、决策变量、可执行模型、网络相关问题、目标函数、链式源与汇、对偶目标、最优值、反转、源到汇、物质流、变体、网络流可视化表示、水、石油和电力

#### 极小化极大问题

#### 最小成本流
二分图、约束条件、决策变量、电力配送成本、可执行模型、目标函数、电力分配模型、发电厂、`Solar-1138 Inc.`、供需、变体

#### 混合整数规划 (`MIP`)
经典问题、设施选址、作业车间调度、多商品流、人员配置水平

#### 多求解器接口 (`MPSolver`)

### N

#### 网络模型
约束条件、厄尔多斯数、整数性、最大流、最小成本流、电影爱好者、取整、最短路径，参见 最短路径、结构描述、转运问题

### O

#### 目标函数

#### 优化问题
最佳路线、目标、模型、`OR-Tools` `MPSolver` `all_different` 谓词、边界提取、决策变量、数组声明、连续二维数组、整数、通用约束声明、高级约束库、声明、目标函数、最优值与解、简单代数约束、求解器实例、求解器调用、求和运算符、包装函数、最优值与解

### P, Q

#### 兼职员工 (`PTE`)

#### 分段函数
约束条件、凸成本函数、定义、可执行模型、最小化非线性函数、非凸目标函数

#### 最优性原理

#### 产品组合问题

#### 项目管理
绝对值问题、备选解、决策变量、可执行模型、图形化表示、房屋建造、极小化极大问题、多目标、最优解、属性、任务

#### 伪国际象棋问题
攻击的车、列与行的提取、实用对角线函数、辅助函数 `maxpiece`、通用模型 `maxrook`、目标函数、位置、车、后与象、运行时间、棋盘尺寸递增

#### 谜题
描述、老虎与淑女、伪国际象棋问题、寄出更多钱！、数独、数学模型中的 `Python` 表达式、文本

### R

#### 原油/粗汽油
混合问题、约束条件、连续性方程、模型、非约束方程、辛烷值、油品风味、精炼汽油、解、稳定、二维

### S

#### 寄出更多钱谜题

#### 集合覆盖问题
二元/指示变量、布尔变量、约束条件、合同、供应商、电动汽车、可执行模型、二元变量、`COIN-OR` 项目、成本函数、决策变量声明 `IntVar` 和 `NumVar`、最优解、求解器实例化、供应商与零件、二维数组、通用发动机、目标函数、供应商与零件、变体、机组调度问题、消防站、电信、病毒检测

#### 集合打包问题
航空公司机组调度、约束条件、决策变量、可执行模型、目标函数、最优解、飞行员、全集与子集、变体

#### 最短路径
决策变量、定义、迪杰斯特拉算法、距离矩阵、可执行模型、谷歌地图、目标函数、最优解、变体、全源函数、无环有向图、弗洛伊德-沃舍尔算法、最长路径、最小化转最大化、最优性原理、乘积、项目管理、关键路径、树模型

#### 肥皂与油
酸含量 (`Aj`)、约束条件、油品成本、决策变量、可执行代码、脂肪酸含量目标、初始库存、目标函数、变体

#### 体育赛事排程
约束条件、数据、决策变量、可执行模型、附加割约束、约束条件、决策变量、实例、队伍数量与每周比赛场次、“每队每周一场比赛”、最优解、有序对生成、参数、职业联赛、松弛、子路径消除约束、目标函数、参数、松弛、收紧、比赛日程、变体

#### 人员配置水平
航空公司、约束条件、`FTE` 必须工作、最低全职员工数、`PTE` 与 `FTE`、时间间隔、决策变量、分解、描述、可执行模型、整数规划、目标函数、需求矩阵、班次、班次类型、太阳系、变体

#### 员工排班
约束条件、课程章节、决策变量、可执行模型、讲师列表、偏好、讲师对、集合、提供的课程列表、目标函数、偏好对、变体

#### 数独
二元变量、约束条件、决策变量、描述、网格位置、辅助函数、指示变量、目标函数、解、变量声明

### T, U, V, W, X, Y, Z

#### 文本

#### 转运问题
特性、流量守恒、约束条件、数据、配送成本、可执行模型、最小成本流、节点、目标函数、最优解、供需、二维变量、变体

#### 旅行商问题 (`TSP`)
电路设计、约束条件、决策变量、描述、距离矩阵、可执行模型、迭代与子路径、主循环、子路径消除约束、子路径提取、连续（部分）解、目标函数、电源供应、变体