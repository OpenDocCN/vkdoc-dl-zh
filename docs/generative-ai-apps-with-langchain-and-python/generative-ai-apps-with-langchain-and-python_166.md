# 云服务

随着你越来越熟练，需求不断增长，你可以探索更高级的选项。

如果第一次没有完美运行，也不必焦虑。部署过程可能很棘手，即使是经验丰富的开发者有时也需要多次尝试才能成功。不知不觉中，你就能像专家一样部署应用了！

## 关键要点

干得漂亮！你刚刚完成了一段从原型到生产的精彩旅程。

你已经使用 `Streamlit` 将最初的 Jupyter 代码转变为一个功能完备的 Web 应用。具体来说，你利用 OpenAI 的 `GPT-3.5-turbo` 模型，创建了一个人们可以与之交互、提问并获得有意义回复的 Web 应用。这太了不起了！

让我们进一步分解你所取得的成就：

1.  你构建了一个交互式问答应用，用户可以与 AI 进行对话！
2.  你掌握了聊天历史管理，使你的应用能够在多次对话中保持上下文。
3.  你学会了部署应用，使其准备好供全世界使用。

## 第 11 章 使用 Streamlit 构建和部署类 ChatGPT 应用

但真正的美妙之处在于，你不仅学会了使用 `LangChain` 和 `Streamlit` 等工具，还获得了直接适用于现实世界 AI 开发的技能。下次当有人说“我们需要一个 AI 驱动的 Web 应用”时，你可以自信地说：“我能行！”

那么，你的 AI 开发之旅下一步是什么？无论是什么，我知道你已经准备好了。继续编码、学习，最重要的是，不断突破生成式 AI 的可能性边界。

## 复习题

测试你对本章的理解。

1.  `Streamlit` 主要用于什么？
    -   A. 数据存储
    -   B. 构建交互式 Web 应用
    -   C. 机器学习模型训练
    -   D. 网络安全

2.  在本章中，哪个 Python 库对于连接到 OpenAI 的 `GPT-3.5-turbo` 模型至关重要？
    -   A. Pandas
    -   B. NumPy
    -   C. LangChain
    -   D. Matplotlib

3.  在本章的上下文中，`Streamlit` 的 `st.text_input` 函数的主要目的是什么？
    -   A. 显示图像
    -   B. 为用户查询创建文本输入字段
    -   C. 管理文件上传
    -   D. 生成图表

4.  在 `Streamlit` 应用中，实现了哪个功能来跟踪对话历史？
    -   A. 会话状态
    -   B. 全局变量
    -   C. 数据库连接
    -   D. 文件日志

5.  部署 `Streamlit` 应用的第一步是什么？
    -   A. 设置 SQL 数据库
    -   B. 编写单元测试
    -   C. 在 Web 服务器或云服务上设置部署环境
    -   D. 创建 Docker 容器

6.  `LangChain` 如何增强应用中 AI 模型的集成？
    -   A. 通过提供预训练的机器学习模型
    -   B. 通过提供简化提示工程和模型管理的工具
    -   C. 通过取代对其他 Python 库的需求
    -   D. 通过改进数据可视化

7.  哪个 `Streamlit` 命令用于在本地运行应用？
    -   A. `streamlit deploy`
    -   B. `streamlit serve`
    -   C. `streamlit start`
    -   D. `streamlit run`

8.  使用 `Streamlit` 开发 AI 应用有什么优势？
    -   A. 它为计算任务提供高性能。
    -   B. 它简化了交互式 Web 界面的创建。
    -   C. 它提供内置的自然语言处理工具。
    -   D. 它默认确保数据安全和加密。

9.  如果你需要在 `Streamlit` 中跨不同用户会话维护聊天历史，应该怎么做？
    -   A. 使用会话 Cookie
    -   B. 实现一个数据库后端
    -   C. 将数据存储在全局变量中
    -   D. 使用本地文本文件

10. 以下哪一项是使用 `LangChain` 集成 OpenAI 的 `GPT-3.5-turbo` 模型的关键步骤？
    -   A. 从头开始训练模型
    -   B. 设置模型温度参数
    -   C. 向模型上传数据
    -   D. 可视化模型架构

## 答案



