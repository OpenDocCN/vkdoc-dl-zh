# 自然语言对话式应用

## A

- `ConversationChain`，处理，40，233，234
- 直接应用的快速原型开发，217
- AI 驱动的功能，41

## D

- `Document Chatbot App`，可扩展应用，41
- `Conversational` 直接调用 OpenAI API，61–65
- `RetrievalChain`，232
- `Document Processing App`，挑战
- `MapReduceChain`，235，236
- 调试与执行，229
- 故障排除，44
- `vs.` `LCEL` 链，218，219
- 延迟，44
- 问答应用，`RetrievalQA`，234，235
- 管理弃用，43
- 速率限制与成本管理，44
- 简洁性，217
- 文本生成应用，`LLMChain`，233
- 安全问题，43
- 选择合适的用例，46
- 类型，230
- 开发环境，55–61
- 用例，218，220

## L

- 法律分析工具，16
- `LengthBasedExampleSelector`，184，185，189，190
- 长度示例选择器，180
- LLM API，商业优势
  - 语言能力的持续提升，41
  - 经济高效地访问大规模语言模型，41
  - 技术优势
    - 高级提示工程能力，43
    - 高效处理上下文与记忆，42
    - 语言模型的灵活集成，42
    - 多模态输入的无缝集成，43
    - 简化复杂的 NLP 任务，42
- `LLMChain`，172，233
  - 来自 `langchain.chains`，68
  - `run` 方法，69
- `"llm_chain"` 模板，221，222
- `load_and_split()` 方法，269
- `load_chain` 函数，219，221
- `load()` 方法，267，268，271
- `load_query_constructor_runnable` 链，227
- `LocalFileStore` 类，294
- `Longformer`，158

## M

- `MapReduceChain`，235，236，245，246，248
- `MarkdownTextSplitter`，275
- 市场分析工具，16
- `最大边际相关性 (MMR) 示例选择器`，179
- `MegatronLM`，157
- 记忆组件，216，253
- 记忆概念，25，53
- Meta AI 模型
  - `DPR`，143
  - `FastText`，144
  - `LLaMA`，143–151
  - `M2M-100`，143
  - `NLLB`，143
  - `OPT`，143
  - `PyTorch`，152
  - `RoBERTa`，143
  - `WaVE`，144
- `Meta-Llama-3-8B` 模型，147，150
- `M2M-100`，143
- 模型微调，168
- 模型输入/输出 (I/O)，76，89，90
- `MRKL Agent`，371
- 多模态与安全模型，132，133
- `多向量检索器`，301，310

## N

- 自然语言处理，40，120，159，361
- 自然语言查询，227，228
- 下一代语言模型，114
- `Ngram 示例选择器`，180，181
- `不让任何语言掉队 (NLLB)`，143

## O

- 可观测性，`LCEL` 链，225
- OpenAI
  - API 密钥，68
  - 聊天模型，203
  - `ChatOpenAI` 模型，416
  - `GPT-3.5-turbo` 模型，484
  - 来自 `langchain.llms`，67
  - `OpenAIEmbeddings` 类，289，295，296
  - OpenAI 函数调用，192，226，417
  - `OpenAIFunctions Parser`，192
  - OpenAI 模型
    - `Codex`，115，116
    - `DALL-E 2`，116–120
    - `GPT`，114，115
  - OpenAI 的 LLM
    - API 密钥，56
    - 账单信息，55
    - 创建/打开账户，55
  - OpenAI 工具代理
    - 优势，379
    - 代理创建，379，380
    - 代理运行，380
    - 聊天历史，381
    - 描述，378
    - 目标，378
    - 初始化工具，379
    - 必要库，379
  - `OpenAITools Parser`，192
- `开放预训练 Transformer (OPT)`，143
- `os.environ.get()`，196
- `OutputFixingParser`，192，198
- 输出解析器，353
  - 函数，191
  - `LLM`，191
  - `OutputFixingParser`，198
  - 使用 `Prompt` 解析，191
  - `PydanticOutputParser`，电影数据
    - 选择 LLM 及其设置，196
    - 创建提示模板，195
    - 定义电影数据模型，194
    - 生成电影信息，197
    - 导入库，194
    - 解析 LLM 的响应，197，198
    - 设置 OpenAI API 密钥，196
  - 类型，192，193

## P, Q

- `page_content` 属性，278
- `PaLM`，1，3，6，8，21–23，28，31
- `PaLM 2`，131
- `父文档检索器`，301，310
- `pc.create_index()` 函数，461
- PDF 文件，25，269，270
- 个性化推荐系统
  - 代理设置，429
  - 代码示例，434
  - 数据预处理与分析，429
  - 定义推荐工具，430
  - 电商平台，428
  - `generate_recommendations` 函数，438
  - 生成个性化推荐，433
  - 初始化代理，433
  - 将推荐集成到应用中，434
  - 推荐提示设置，432
  - 步骤，164–169
  - 工具设置，432
  - `vs.` 模型微调，168
- 提示，76
  - 链式调用，53
  - 组件
    - 输出解析器（参见输出解析器）
    - 提示模板，170–174
  - 简化客户服务，案例研究，203，204
    - 高级工程，206
    - 定制化，205



