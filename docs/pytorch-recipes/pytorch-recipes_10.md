# 索引

## A

激活函数 双线性函数 定义 双曲正切函数 带泄露修正线性单元 线性函数 对数 S 型传递函数 `PyTorch` *vs.* `TensorFlow` `ReLU` S 型函数 可视化，形状 `Adadelta` `Adagrad` `Adam` 优化器 算法分类 备选数据收集 人工神经网络（ANN） 自编码器 架构 聚类模型 编码数据，3D 图 特征 双曲正切函数 层 MNIST 数据集 `torchvision` 库 `Autograd`

## B

伯努利分布 贝塔分布 双线性函数 二项分布

## C

中央处理器（CPU） 计算图 计算语言学 连续词袋（CBOW） 示例 表示 向量 嵌入 连续均匀分布 卷积神经网络（CNN） 架构 计算过程 超参数 加载器功能 MNIST 数据集 `net1` 对象 pickle 文件格式 池化层 预测服务 预测过程 `restore` 函数 `restore_net()` 函数 `save` 函数 测试准确率 训练损失

## D, E

数据挖掘 深度学习模型 批量大小 批量训练 CPU 环境 损失函数 在线训练 超参数 学习率 并行数据训练 序列神经网络 深度神经网络（DNN） 离散概率分布 双指数分布

## F

Facebook 的人工智能

## G

`GloVe` 梯度计算 梯度下降算法 图形处理器（GPU）

## H

双曲正切函数

## I, J

实现，深度学习 安装，`PyTorch`

## K

关键词搜索应用



## L

- 语言翻译
- 拉普拉斯分布
- 带泄露修正线性单元 (Leaky ReLU)
- 词汇歧义
- 线性函数
- 线性回归假设、公式、梯度下降算法、身高与体重的均值、标准差和协方差、多元线性回归模型、OLS 方法、普通最小二乘法、模型预测误差、预测建模、简单线性回归模型、逻辑回归模型的设定
- 对数 S 型传递函数
- 长短期记忆 (LSTM) 模型
- 损失函数
  - `backward()` 函数
  - 训练轮次 (epochs)
  - 估计参数
  - 最终损失值
  - `grad` 函数
  - 超参数
  - 初始值
  - 迭代层级
  - 学习率
  - 线性方程计算
  - 均方误差 (MSE)
  - `MSELoss`
  - 参数网格
  - 权重张量

## M

- 机器学习
- 均值计算
- 多维张量
- 多项分布
- 多元线性回归模型
- 多进程处理

## N

- 自然语言生成
- 自然语言处理应用的五步法
- 自然语言理解
- 网络架构
- 神经网络 (NN)
  - 激活函数 ( *参见* 激活函数)
  - 架构
  - 数据挖掘
  - 数据准备
  - 定义
  - 设计
  - 误差函数
  - 功能
  - 中位数、众数和标准差
  - 模块
  - `Net()` 函数
  - 网络架构
  - 优化函数
    - Adadelta
    - Adagrad
    - Adam
    - ASGD
    - RMSprop 算法
    - SGD
    - SparseAdam
  - ReLU 激活函数
  - `set_weight()` 函数
  - 逐步方法
  - 结构
  - 张量微分
  - `torch.nn` 包
- n-gram 语言模型
- 正态分布
- 基于 NumPy 的操作

## O

- 优化模块
- 优化函数
  - Adadelta
  - Adam
  - 反向传播过程
  - 训练轮次 (epochs)
  - 梯度
  - 损失函数
  - 参数
  - 预测张量
  - 回归线
  - `Tensor.backward()`
  - 张量值
  - `torch.no_grad()`
  - 训练集
  - 验证数据集
- 普通最小二乘模型

## P

- 预测文本
- 概率分布
  - 自编码器 ( *参见* 自编码器)
  - CNN 损失函数 ( *参见* 损失函数)
  - 数学运算
  - 模型过拟合
    - 丢弃率超参数
    - 过拟合损失与丢弃损失
  - 参数
  - 预测值
  - 训练准确率
  - 训练数据集
  - 优化函数
  - RNN 类型
  - 权重、丢弃率

## Q

- 问答系统

## R

- 修正线性单元 (ReLU)
- 循环神经网络 (RNN)
  - Adam 优化器
  - 内置函数
  - `dsets.MINIST()` 函数
  - 嵌入层
  - 超参数
  - 图像数据集
  - LSTM 模型
  - 记忆网络
  - MNIST 数据集
  - 预测
  - 回归问题
    - `cos` 函数
    - 非线性周期模式
    - 输出层
    - 测试准确率
    - 测试数据
    - 时间序列
    - 权重
  - Word2vec
- 指代歧义
- 回归学习
- RMSprop 算法

## S

- 情感分析
- 序列神经网络
  - 类 `Net`
  - 函数式 API
  - 超参数
  - Keras 函数
  - 模型架构
- Sigmoid 函数
- 简单线性回归模型
- Skip-gram
- SparseAdam
- 标准差
- 统计分布
- 统计学
- 随机梯度下降 (SGD)
- 随机变量
- 监督学习
  - 计算图网络
  - 数据准备
  - 前向与反向传播 ( *参见* 神经网络 (NN))
  - `grad()` 函数
  - 线性回归 ( *参见* 线性回归)
  - 逻辑回归模型
  - 方法
  - `mtcars.csv` 数据集
  - `nn.backward()` 方法
  - 优化与梯度计算
  - 训练数据
- 句法歧义

## T

- 张量微分
- TensorFlow 函数
- 张量
  - `arrange` 函数
  - `clamp` 函数
  - 数据结构
  - 维度
  - `is_storage` 函数
  - `is_tensor` 函数
  - 对数值
  - `LongTensor`/`index` 选择函数
  - 数学函数
  - NumPy 函数
  - 一维 `split` 函数
  - 变换函数
  - 二维 `unbind` 函数
  - 均匀分布
- 文本分析
- 文本摘要
- 分词
- 主题建模
- 训练数据

## U, V

- 无监督学习
- 工具函数
- 工具模块 (Utils)

## W, X, Y, Z

- 权重初始化
- Word2vec
- 词嵌入
  - 上下文提取
  - 定义
  - 示例
  - n-gram 提取器
  - 向量格式
